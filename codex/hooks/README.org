#+title: Codex hook implementations
#+author: Pablo Stafforini

* Activation and boundaries

The tracked [[file:../hooks.json][hooks.json]] file registers Codex hooks and is live globally through
the =~/.codex/hooks.json= symlink. Registration and implementation are separate:
the JSON chooses an event and matcher, while the scripts in this directory
decide what to block, record, regenerate, or report. Five =lib-*= files are
sourced helpers rather than registered hooks. =session-learning-capture.sh= is
a retained legacy helper and is not registered at present.

These hooks supplement the written instructions with executable checks. An
allowing result from one hook does not bypass another hook, the global rules,
or the need for user authorization.

* Operational contracts

The destructive-command guard is registered directly here. Claude carries the
same logic inline in its =pretooluse-bash.sh= dispatcher, so both implementations
must change and be tested together. Decisions are allow or deny, never approval
prompts; =ALLOW_CLONE=1= is the narrow explicit clone override.

The dirty-tree rewrite guard blocks =filter-repo= and =filter-branch= only when
the target repository has staged or unstaged tracked changes. Untracked files
are deliberately ignored. It resolves =git -C= and the last applicable =cd=
before falling back to the hook working directory, and masks heredoc bodies to
avoid prose false positives. =ALLOW_DIRTY_REWRITE=1= is the explicit override.

The sensitive-read guard denies content access to credential-bearing paths. It
permits content-free metadata, quiet/count/name-only searches, existence tests,
Git ignore/index queries, and auth-aware tools only in recognized narrow forms.
It also permits =.env= or =.envrc= to be sourced solely as subprocess
environment, while still denying commands that print, parse, or dump it.

The secret-leak guard applies the same command policy to every literal nested
=cmd= inside a =functions.exec= program, including nested shell-tool calls.
Read-only Automations access must use =op-automations= and desktop-gated work
must use =op-desktop=. The broker forwards piped item templates, so stdin is not
a reason to bypass it. Raw =op= writes and =--reveal= remain denied.

The Elisp commit gate routes each literal nested =tools.exec_command= call by
its own =workdir=, groups staged and commit calls by repository, inherits an
omitted workdir from the outer context, and denies dynamic or ambiguous commit
targets. The post-commit tracker uses the same context and recognizes Git
global options such as =-C=. Multiple commits inside one shell call remain
distinct. Inert quoted text and shell comments do not count as commits, while
interpreter payloads remain conservative. Only evidence whose label matches
exactly one supported wrapper in executable position, with no other executable
in that command event, can clear an obligation.

Elisp test and live evidence includes a one-time correlation receipt. The
tracker binds the evidence label to the observed wrapper arguments and
atomically consumes the receipt. A receipt can clear one matching event, and a
second use fails. A textual mention, mismatched label, or compound output
command does not count as the wrapper event. This is not authentication against
another process running as the same user: such a process can source the public
issuer or write the user-owned state files. On the next issue or consume
operation, the helper removes only user-owned regular receipt and claim files
older than one day. It never expires or removes test markers or
live-verification obligations.

The Elisp documentation gate honors
=branch.<branch>.deferDocUpdates=true= only on feature branches and ignores it
on =main= and =master=. Remove the setting when the branch merges.

The hook installer owns all eight =codex-hook-wrapper= lifecycle registrations.
It must preserve the live =config.toml= and =hooks.json= symlinks and replace
stale profile-specific wrapper paths rather than append duplicates. The global
wrappers are safe outside Emacs: when =CODEX_BUFFER_NAME= is absent, they
consume the payload and exit successfully without calling =emacsclient=.

=require-codex-readme-update.sh= is a dotfiles-scoped commit gate. Its
command-text fallback excludes project-local =.codex/= paths, and it excludes
the repo-root =AGENTS.md= project mirror; the project-local dotfiles skill is
handled by its configured documentation owner instead.

* File index

| File | Purpose |
|------+---------|
| =block-destructive-command.sh= | Blocks hard-to-reverse shell operations, with quoted-text masking to avoid treating prose as a command. |
| =block-dirty-tree-rewrite.sh= | Blocks Git history rewrites when the target repository has staged or unstaged tracked changes. |
| =block-elpaca-dotfiles-edit.sh= | Denies writes to the read-only Elpaca dotfiles mirror and points to the canonical repository. |
| =block-elpaca-rebuild-uncommitted.sh= | Blocks rebuilding an extras package while its canonical Elisp source has uncommitted changes. |
| =block-github-write-command.sh= | Blocks Git pushes and recognized mutating =gh= operations unless the target matches an exact repo or account wildcard in the committed allowlist, or an exact Epoch project repo in the fixed committed automations registry. This includes topic-branch pushes, pull-request and issue creation or edits, and comments. Read-only inspection remains allowed. Nested =functions.exec= shell calls are checked independently and ambiguous recognized calls fail closed. |
| =block-secret-leak.sh= | Blocks literal secret disclosure and unsafe secret-bearing reads, writes, or output. |
| =block-sensitive-read.sh= | Blocks content reads of credential-like files while permitting narrowly defined metadata checks and the exact value-free shell-export classifier. |
| =block-texi-edit.sh= | Blocks direct edits to generated Texinfo and points to the Org source. |
| =block-unguarded-ahrefs-api.sh= | Blocks paid Ahrefs API calls that do not pass through the quota guard. |
| =block-walk-list-access.sh= | Restricts direct access to the protected walk-list data store. |
| =lib-codex-hook-json.sh= | Normalizes Codex hook payloads, extracts literal nested command/workdir records without evaluating JavaScript, recognizes Git subcommands after supported global options, and recovers an exact nested Bash command's parent workdir from the session transcript. |
| =lib-codex-paths.sh= | Extracts changed paths from the supported Codex edit and patch payload forms. |
| =lib-elisp-evidence.sh= | Issues and atomically consumes one-time correlation receipts for Elisp test and live evidence; these receipts prevent replay but do not authenticate against the same user. |
| =lib-repo-root.sh= | Resolves the effective Git repository from structured hook context, Git global options, or an exact unambiguous parent =functions.exec= record without shell re-evaluation. |
| =lib-staged-files.sh= | Lists staged paths and name-status data, including an amended commit as the complete proposed commit. |
| =load-elisp-after-edit.sh= | Rebuilds and reloads the owning Elpaca package after an Elisp edit, including paths recovered from a nested composed-tool patch; skips test-only files and active or unresolved Git operations. |
| =regenerate-manual-after-edit.sh= | Regenerates Texinfo and existing Info output after an Org manual edit. |
| =remind-ai-config-sync.sh= | Delegates paired Claude/Codex update reminders to =bin/ai-config-sync=. |
| =remind-claude-readme-update.sh= | Reminds work outside dotfiles when a symlinked edit changed the Claude configuration. |
| =remind-plan-review.sh= | Reminds the agent to obtain an independent review after editing an implementation plan. |
| =require-ai-config-sync.sh= | Delegates paired-configuration commit blocking to =bin/ai-config-sync=. |
| =require-codex-readme-update.sh= | Requires the Codex README with staged Codex integration or sync changes. |
| =require-doc-update.sh= | Requires the owning manual and generated Texinfo with staged documented Elisp changes. |
| =require-elisp-test-before-commit.sh= | Routes direct and composed commits to the target repository, blocks combined staging, and requires matching package or deletion-check evidence for the proposed Elisp source. |
| =require-elisp-verify-after-commit.sh= | Allows only exact rebuild/live helpers until repository/package/commit-bound evidence clears an Elisp commit. |
| =require-readme-update.sh= | Requires the Claude README with staged Claude skills, hooks, or settings changes. |
| =session-learning-capture.sh= | Retained unregistered helper for requesting session-learning capture at session end. |
| =track-elisp-test.sh= | Consumes a one-time receipt, then validates and records successful repository/package/source-revision evidence from the source-aware batch runner. |
| =track-elisp-verify.sh= | Records every production package across direct or composed Elisp commits and consumes one-time receipts before clearing matching bound live evidence. |

The documentation audit requires every tracked direct child to appear exactly
once in this table. After changing a registered hook, verify the registration
and exercise the relevant allow and deny paths; running the script in isolation
does not prove that Codex invokes it with the intended event payload.
