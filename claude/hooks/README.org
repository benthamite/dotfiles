#+title: Claude hook implementations
#+author: Pablo Stafforini

* Activation and boundaries

Claude's global hook registration lives in the mutable
=~/.claude/settings.json= file. It invokes selected scripts here by absolute
path; there is no =~/.claude/hooks= directory link. The tracked
=ai-config-sync.json= manifest owns and audits the paired hook inventory.

Not every file below is a global event registration. Some are sourced helpers,
dispatch targets, one-shot installers, compatibility wrappers, or project-local
guards. Registration determines when a script runs; its presence in this
directory does not.

* Operational contracts

The destructive-command policy is registered as a standalone Codex hook but is
inlined into Claude's =pretooluse-bash.sh= dispatcher. Change and test those two
implementations together. Both are deny-only and never ask for approval;
=ALLOW_CLONE=1= is the narrow explicit override for an otherwise blocked clone.

The dirty-tree rewrite guard blocks =filter-repo= and =filter-branch= only when
the target repository has staged or unstaged tracked changes. Untracked files
are deliberately ignored. It resolves the target from =git -C=, the last
applicable =cd=, and then the hook working directory, while masking heredoc
bodies to avoid treating quoted examples as commands. =ALLOW_DIRTY_REWRITE=1=
is its explicit override.

The sensitive-read guard denies content access to credential-bearing paths.
It permits content-free metadata commands, quiet/count/name-only searches,
existence tests, Git ignore/index queries, and auth-aware tools only within the
narrow forms recognized by the hook; a compound command cannot inherit an
allowance from a safe first command. Sourcing a quoted or unquoted =.env= or
=.envrc= solely as subprocess environment is allowed, including =set -a=
wrappers, but printing, parsing, or dumping that environment is denied. Claude
registers the standalone hook for =Read= and keeps its Bash logic inlined in
=pretooluse-bash.sh=.

The secret-leak guard blocks secret-shaped literals, unsafe output routes, and
direct =op= use. Read-only Automations access goes through =op-automations=;
desktop-gated work goes through =op-desktop=. A missing broker is an error to
repair, not permission to bypass routing with raw =op=. =--reveal= remains
denied inside sanctioned forms.

The Elisp documentation gate may be deferred on a feature branch with
=branch.<branch>.deferDocUpdates=true= for a deliberate multi-commit refactor.
The setting is ignored on =main= and =master= and should be removed when the
branch merges. The Codex counterpart uses the same rule.

Elisp test and live evidence includes a one-time correlation receipt. The
tracker binds the evidence label to exactly one supported wrapper in executable
position, rejects command events with another executable, and atomically
consumes the receipt. A receipt can clear one matching event, and a second use
fails. A textual mention, mismatched label, or compound output command does not
count as the wrapper event. This is not authentication against another process
running as the same user: such a process can source the public issuer or write
the user-owned state files. On the next issue or consume operation, the helper
removes only user-owned regular receipt and claim files older than one day. It
never expires or removes test markers or live-verification obligations.

The reasoning-worktree SessionStart hook repairs required key links before its
working-directory and origin checks. That repair loop therefore runs in every
session in every repository, so it must start no subprocess when all links are
already correct. It uses shell parameter expansion for the worktree name and the
=-ef= test for the already-correct comparison, instead of one =basename= and one
=readlink= process for each worktree. A link that reaches the canonical key
through a different path counts as correct and stays as it is; a missing,
broken, or foreign link is still repaired. For eligible worktrees it fetches and
merges shared state without pushing, reapplies the local overlay and context,
injects private project skills, and installs the worktree-local issue-comment
freshness guard. The bulk wrapper performs the shared fetch once. As best-effort
nonfatal maintenance, the hook also garbage-collects only clean, fully pushed
transient CR worktrees and prints the =cr-task-state --hook= branch, run, and
rubric-state banner with divergence warnings. Comment freshness blocks a post
when a newer reasoning-task issue comment has not been acknowledged and includes
that newest comment in the denial; API failures are an intentional fail-open
boundary. Codex has no SessionStart counterpart and routes this posting flow to
Claude.

The LaunchAgent registry guard routes every launchd change through
=~/repos/launchd=. It denies writing a plist into
=~/Library/LaunchAgents=, redirecting into that directory, and
=launchctl bootstrap/load/enable/submit= as well as
=bootout/unload/disable/remove=, and names =bin/launchd-install.py= in the
denial. That script writes the canonical plist, symlinks the live file, adds the
=registry/jobs.json= entry, and runs the drift audit; loading stays behind its
explicit =--load= flag. Read-only inspection is deliberately untouched: =ls=,
=cat=, =readlink=, =diff=, =plutil -p=, =defaults read=, and =launchctl
print/list=. Heredoc bodies are masked so a commit message quoting a live path is
not read as an invocation. =ALLOW_LAUNCHAGENT_CHANGE=1= is the narrow override
for a change Pablo has authorized, such as a temporary bootout while debugging.

* File index

| File | Purpose |
|------+---------|
| =block-destructive-command.sh= | Blocks hard-to-reverse shell deletion and destructive operations. |
| =block-dirty-tree-rewrite.sh= | Blocks Git history rewrites when the target repository has dirty tracked changes. |
| =block-elpaca-dotfiles-edit.sh= | Denies writes to the read-only Elpaca dotfiles mirror and points to the canonical repository. |
| =block-elpaca-rebuild-uncommitted.sh= | Blocks rebuilding or reloading when the relevant Elisp source is uncommitted. |
| =block-github-write-command.sh= | Blocks Git pushes and recognized mutating =gh= operations unless the target matches an exact repo or account wildcard in the committed allowlist, or an exact Epoch project repo in the fixed committed automations registry. This includes topic-branch pushes, pull-request and issue creation or edits, and comments. Read-only inspection remains allowed. |
| =block-long-pipeline-run-unchecked.sh= | Requires preflight, cleanliness, and checkpoint conditions for long consensus-trader jobs. |
| =block-secret-leak.sh= | Blocks secret literals, unsafe secret-file writes, and secret-bearing output. |
| =block-sensitive-read.sh= | Blocks content reads of credential-like paths while permitting narrow metadata checks and the exact value-free shell-export classifier. |
| =block-texi-edit.sh= | Blocks direct edits to generated Texinfo and points to the Org source. |
| =block-unguarded-ahrefs-api.sh= | Blocks paid Ahrefs calls outside the guarded quota wrapper. |
| =block-unregistered-launchagent.sh= | Routes LaunchAgent installs, loads, and removals through the =~/repos/launchd= registry and its installer. |
| =block-walk-list-access.sh= | Prevents bypassing the walk-list skill's protected data store. |
| =cr-card-body-gate.sh= | Validates a CR issue body file before a GitHub create or edit operation. |
| =cr-issue-comment-freshness.sh= | Requires acknowledgement of the latest reasoning-task issue comment before a project-local post. |
| =cr-skill-routing-reminder.sh= | Reminds an agent to use the owning CR authoring or review skill. |
| =elpaca-run.sh= | Resolves the active Emacs profile and runs an executable from an Elpaca package. |
| =fire-and-forget.sh= | Detaches nonblocking Notification and Stop hook work after consuming the event payload. |
| =lib-elisp-evidence.sh= | Issues and atomically consumes one-time correlation receipts for Elisp test and live evidence; these receipts prevent replay but do not authenticate against the same user. |
| =lib-repo-root.sh= | Resolves the effective Git root from command context and supported Git global options. |
| =lib-staged-files.sh= | Lists staged paths and name-status data, including an amended commit as the complete proposed commit. |
| =load-elisp-after-edit.sh= | Rebuilds and reloads an edited Elisp package through the live Emacs session, except for test-only files or while Git has an active operation or unmerged index entries. |
| =notify-emacs-notification.sh= | Forwards a Claude Notification event to Emacs. |
| =notify-emacs-session-end.sh= | Forwards a Claude Stop event to Emacs. |
| =pretooluse-bash.sh= | Dispatches the Bash guards and output wrapper registered for PreToolUse. |
| =redact-secrets.sh= | Masks recognized secret literals in a command's combined output. |
| =regenerate-coverage-map.sh= | Refreshes the CR rubric visualizer coverage map after relevant edits. |
| =regenerate-manual-after-edit.sh= | Regenerates Texinfo and existing Info output after an Org manual edit. |
| =register-coverage-hook.py= | Idempotently installs the coverage-map hook registration. |
| =remind-ai-config-sync.sh= | Issues the paired Claude/Codex configuration reminder after relevant edits. |
| =remind-claude-readme-update.sh= | Reminds an agent when a write from any project changed the tracked Claude configuration. |
| =remind-plan-review.sh= | Reminds an agent to obtain an independent review after editing an implementation plan. |
| =require-ai-config-sync.sh= | Blocks commits that violate the paired Claude/Codex configuration contract. |
| =require-doc-update.sh= | Requires the owning package manual with staged documented Elisp changes. |
| =require-elisp-skill.sh= | Blocks Elisp edits until the Elisp conventions skill has been loaded. |
| =require-elisp-test-before-commit.sh= | Routes commits to the target repository, blocks combined staging, and requires matching package or deletion-check evidence for the proposed Elisp source. |
| =require-elisp-verify-after-commit.sh= | Blocks later commands until the exact helper emits repository/package/commit-bound live evidence. |
| =require-readme-update.sh= | Requires Claude configuration documentation with staged Claude configuration changes. |
| =session-learning-capture.sh= | Retained unregistered Stop helper for requesting reusable-lesson capture. |
| =sync-agent-c-worktree.sh= | Compatibility wrapper for the renamed reasoning-task worktree synchronization hook. |
| =sync-reasoning-tasks-worktree.sh= | Repairs and updates an eligible reasoning-task worktree at SessionStart without pushing it. |
| =track-elisp-skill.sh= | Records that the Elisp conventions skill was loaded in the current session. |
| =track-elisp-test.sh= | Consumes a one-time receipt, then validates and records repository/package/source-revision evidence from the source-aware batch runner. |
| =track-elisp-verify.sh= | Records each production package in an Elisp commit and consumes one-time receipts before clearing matching repository/package/commit live evidence. |
| =wrap-bash-output.sh= | Routes plausible secret-printing command output through the redactor while preserving status. |

The documentation audit requires every tracked direct child to appear exactly
once in this table. After changing a registered hook, verify its mutable global
registration and exercise the relevant allow and deny behavior through Claude;
a successful standalone invocation does not prove event integration.
