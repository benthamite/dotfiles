#+title: Codex configuration
#+author: Pablo Stafforini

* Overview

This directory contains the version-controlled parts of my [[https://developers.openai.com/codex/][Codex]] setup.
The matching Claude Code setup remains the source of truth where the two tools
can share content directly.  Codex gets explicit ports only where Codex syntax
or discovery rules require them.

The current synchronization model is deliberately not automatic.  The ports are
real files that an agent updates in the same session when it changes the
corresponding Claude file.  Hooks remind the active agent immediately and block
one-sided commits unless the counterpart or =ai-config-sync.json= is staged.

* Current layout

#+begin_example
codex/
├── AGENTS.md              -> ../claude/CLAUDE.md
├── config.toml            <- live ~/.codex/config.toml symlink target
├── bin/install-skills.sh  <- links tracked skills into ~/.codex/skills
├── hooks/                 <- Codex hook script ports
├── README.org
├── rules/default.rules
└── skills/                <- Codex ports of Claude skills

.codex/hooks.json          <- live ~/.codex/hooks.json symlink target
ai-config-sync.json        <- Claude/Codex port manifest
bin/ai-config-sync         <- audit, reminder, and commit-guard checker
#+end_example

* Instructions

=claude/CLAUDE.md= is the global instruction source.  =codex/AGENTS.md= is a
symlink to that file, so Codex sees the Claude content through the filename it
expects.  Project-level =CLAUDE.md= files are still loaded by Codex because
=codex/config.toml= sets:

#+begin_example
project_doc_fallback_filenames = ["CLAUDE.md"]
#+end_example

Codex does not expand Claude-style =@file= imports in Markdown instructions.
For that reason, the repo-root =AGENTS.md= remains a manual Codex-readable port
of the repo-root =CLAUDE.md= rules, whose =@claude/skills/...= import would not
be expanded by Codex.

* Skills

=codex/skills/= contains one Codex port for each Claude skill under
=claude/skills/=.  The one-time port copies the skill bodies and auxiliary
files, then strips Claude-only frontmatter keys such as =user-invocable=,
=allowed-tools=, =argument-hint=, and =model=.

Codex discovers global skills from =~/.codex/skills/*/SKILL.md=.  The tracked
ports therefore have to be linked into that directory.  Run:

#+begin_src sh
codex/bin/install-skills.sh
#+end_src

The script creates one symlink per tracked skill and leaves unrelated local
skills, such as project handoff skills, alone.

When changing a skill:

1. Edit the side requested by the user.
2. Update the counterpart in the same session.
3. Stage both sides, or stage =ai-config-sync.json= with a clear status or
   exemption if a counterpart is intentionally absent.

Skill files must be named uppercase =SKILL.md=.  This matters on the Google
Drive mount and for Codex skill discovery.

* Hooks

=.codex/hooks.json= is tracked here and live globally through the symlink at
=~/.codex/hooks.json=.  It registers Codex-side scripts from =codex/hooks/=
instead of calling Claude hook scripts directly.

The Codex ports cover:

- Bash safety and workflow gates: secret leakage, destructive commands,
  walk-list data access, Elisp test-before-commit, manual update checks,
  post-commit Elisp verification, and stale elpaca rebuild protection.
- MCP guardrails through =guard-external-actions.sh=.
- =apply_patch=/=Edit|Write= guards for secret leakage, generated Texinfo
  files, elpaca dotfiles mirror edits, and walk-list protected data.
- Post-edit behavior for Elisp reloads, generated manual regeneration, README
  reminders, and Claude/Codex sync reminders.
- Codex Stop and SessionStart forwarding to Emacs via
  =codex/hooks/notify-emacs-hook.sh=.

Some Claude hooks are intentionally not ported one-for-one because Codex does
not expose the same event in this setup:

- Claude =Read=/=Grep=/=Glob= hooks such as =block-sensitive-read.sh=.
- Claude =Skill= hooks such as =require-elisp-skill.sh= and
  =track-elisp-skill.sh=.
- Claude Notification-specific Emacs hooks.  Codex uses
  =notify-emacs-hook.sh= for Codex lifecycle events instead.

* Sync Guard

=ai-config-sync.json= is the manifest for the port.  It lists every skill, the
hook port status, live registration files, and the current unsupported or
Codex-only cases.

Two hook pairs keep the model simple:

- =claude/hooks/remind-ai-config-sync.sh= and
  =codex/hooks/remind-ai-config-sync.sh= run after edits and print the
  counterpart that should be updated.
- =claude/hooks/require-ai-config-sync.sh= and
  =codex/hooks/require-ai-config-sync.sh= run before =git commit= and block
  one-sided Claude/Codex config changes unless the counterpart or manifest is
  staged.

Manual audit:

#+begin_src sh
bin/ai-config-sync audit
#+end_src

The audit is deterministic; it does not edit files.  Its job is to make drift
visible.

* Global Activation

=~/.codex/config.toml= points to =codex/config.toml=, and
=~/.codex/hooks.json= points to =.codex/hooks.json=.  Updating the tracked files
therefore updates the live global Codex configuration.

=~/.codex/skills/= remains a normal Codex home directory for system or local
skills.  The mirrored Claude skill ports are symlinked into that directory by
=codex/bin/install-skills.sh= because Codex does not load arbitrary skill paths
from =codex/config.toml=.
