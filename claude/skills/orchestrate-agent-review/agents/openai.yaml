interface:
  display_name: "Agent Review Orchestrator"
  short_description: "Coordinate one-way spec and plan reviews"
  default_prompt: "Use $orchestrate-agent-review to coordinate one spec review and one plan review, then return implementation to the same authoring agent."
