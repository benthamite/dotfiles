#+title: Claude Code configuration
#+author: Pablo Stafforini

* Claude Code configuration
:PROPERTIES:
:ID:       8A661AE9-E263-4E1B-9969-44C61B202DA4
:END:
** Overview
:PROPERTIES:
:ID:       FC7EF2FA-CDB3-43C0-9EFC-545DEA807191
:END:

This directory contains my [[https://docs.anthropic.com/en/docs/claude-code][Claude Code]] configuration: settings, hooks, and custom skills. Claude Code is a CLI-based coding agent; I use it from the terminal alongside Codex.

The cross-tool synchronization policy with Codex is documented in
=../agents/README.org=. This file covers the Claude Code-specific configuration
topology and inventory.

*** Architecture
:PROPERTIES:
:CUSTOM_ID: h:architecture
:ID:       AE7C697F-AF66-44E4-BBEA-85801E3B0895
:END:

**** Directory topology
:PROPERTIES:
:CUSTOM_ID: h:directory-topology
:ID:       EB7422C8-E371-4F9B-AE1F-D6B6473F11BB
:END:

Three Claude-specific layers work together: this dotfiles directory (source of
truth), the shared =~/.claude/= hub, and per-account overlay directories. The
authoritative cross-tool path-role table lives in =../agents/README.org=.

*Source of truth* (version-controlled in this repo):

#+begin_example
~/My Drive/dotfiles/claude/           ← global config source of truth
├── CLAUDE.md                         ← global conventions for all projects
├── hooks/                            ← hook scripts
├── skills/                           ← skill directories
├── bin/batch-test.sh                 ← elpaca-aware emacs --batch test runner
├── bin/sync-mcp-servers.sh           ← MCP server sync utility
├── bin/gmail.py                      ← Gmail API CLI (--account epoch|personal; attachments supported for send/draft/reply)
├── bin/sheets.py                     ← Sheets API CLI (--account epoch|personal)
├── bin/slack.py                      ← Slack web-API CLI, including permalink/thread fetches
├── bin/_gworkspace_auth.py           ← shared OAuth helper for gmail.py / sheets.py
├── ../bin/ai-config-sync             ← Claude/Codex sync audit and hook checker
└── README.org                        ← this file
#+end_example

Note: =settings.json= is intentionally *not* tracked here. Claude Code writes to =~/.claude/settings.json= directly (via =/theme=, =/effort=, =/permissions=, plugin toggles, etc.), and those atomic writes would break any symlink back to this directory. The file is the canonical live config; edit it in place.

*Shared hub* (=~/.claude/=):

#+begin_example
~/.claude/
├── CLAUDE.md          → dotfiles/claude/CLAUDE.md (symlink)
├── skills/            → dotfiles/claude/skills/ (symlink)
├── settings.json      ← canonical, mutable, not tracked
├── settings.local.json ← machine-specific, not tracked
├── projects/          ← trust entries, session logs (managed by Claude Code)
├── history.jsonl      ← command history (managed by Claude Code)
└── plugins/           ← installed plugins (managed by Claude Code)
#+end_example

*Per-account overlays* (=~/.claude-personal/=, =~/.claude-tlon/=, =~/.claude-epoch/=): see [[*Multi-account setup][multi-account setup]] below.

*Per-project config* (=<project>/.claude/=):

#+begin_example
<project>/.claude/
├── settings.json       ← project-level hooks (optional)
├── settings.local.json ← project-specific permissions (accumulated interactively)
└── skills/             ← project-specific skills (optional)
#+end_example

*Cross-tool topology.* The authoritative path-role table lives in
=../agents/README.org=. In particular, repo-root =.claude/= and =.codex/= paths
are project-local by definition and must not be used as targets for global
=~/.claude/= or =~/.codex/= symlinks.

*MCP server reference.* Placement rules, current inventory, multi-account notes, account-specific credential handling, identity checks, and reload requirements for remaining project-local exceptions live in =context/mcp-servers.md=. The global =CLAUDE.md= points agents there when MCP-specific context is needed, so they do not need to read this whole README just to configure or debug an MCP server.

*Service access tools.* The global =CLAUDE.md= lists the local tools used to connect to external services: Anna's Archive, Twitter/X, Gmail, Google Sheets, Slack, Google Calendar, Google Docs/Drive, and GitHub. These local tools are the default path for shared Claude/Codex workflows; MCP servers and hosted connectors are exceptions documented in =context/mcp-servers.md= and service-specific context files.

**** Settings hierarchy
:PROPERTIES:
:CUSTOM_ID: h:settings-hierarchy
:ID:       ABB0C5CA-1B40-47BF-8746-BFFE1F4E420D
:END:

Claude Code loads settings from multiple levels, merged together. Each level can define permissions, hooks, and other options. Permissions are additive across levels. Hooks run from all levels that define them.

| Level | =settings.json= | =settings.local.json= | Purpose |
|-------+------------------+-----------------------+---------|
| User (=~/.claude/=) | Model, theme, global hooks, global permissions | Machine-specific tool whitelists | Applies to all projects |
| Project (=<project>/.claude/=) | Project-specific hooks | Project-specific permissions | Applies only in that project |

The user-level =~/.claude/settings.json= and
=~/.claude/settings.local.json= files are not version-controlled; both are
edited in place in =~/.claude/=.

The global =settings.json= uses =auto= mode (Anthropic's classifier-backed permission mode that approves most actions while flagging risky ones) inside the =permissions= object. All MCP servers are explicitly listed in =permissions.allow= as a workaround for [[https://github.com/anthropics/claude-code/issues/5140][anthropics/claude-code#5140]]. Safety hooks (=guard-external-actions.sh=, =block-destructive-command.sh=, etc.) provide additional guardrails.

**** =CLAUDE.md= loading
:PROPERTIES:
:CUSTOM_ID: h:claude-md-loading
:ID:       D03891CF-C905-480A-9690-62B4F8A7273D
:END:

Claude Code loads =CLAUDE.md= files from several locations. When working in a project, it loads:

1. =~/.claude/CLAUDE.md= — user-level global conventions (symlinked to =dotfiles/claude/CLAUDE.md=)
2. =<project>/CLAUDE.md= — project-level instructions (if present)

Each file should contain distinct content. The global one defines behavioral conventions (verify work, fix root causes, commit immediately, etc.). Project-level ones define project-specific context.

**** Where to put things
:PROPERTIES:
:CUSTOM_ID: h:where-to-put-things
:ID:       A01A5164-CE5F-414D-9999-924B5C89339B
:END:

Use this table for Claude-specific placement. For reusable artifacts that should
work in both Claude and Codex, =../agents/README.org= is authoritative.

| What | Where | Why |
|------+-------+-----|
| New global Claude behavioral rule | =claude/CLAUDE.md= | Applies to all Claude projects; update =codex/AGENTS.md= when it should also apply to Codex |
| New project-specific instruction | =<project>/CLAUDE.md= | Scoped to that project; update the sibling =<project>/AGENTS.md= as the Codex counterpart |
| New reusable skill or hook | Follow =../agents/README.org= | Shared artifacts need a counterpart or explicit manifest exemption |
| New Claude global hook registration | =~/.claude/settings.json= | Fires in all Claude projects; mutable live config edited in place |
| New Claude project-specific hook registration | =<project>/.claude/settings.json= | Fires only in that project |
| New machine-specific permission | =~/.claude/settings.local.json= | E.g. brew, mpv, pip3 |
| New MCP server | =~/.claude.json=, then run =claude/bin/sync-mcp-servers.sh= | Syncs to account dirs |
| New MCP reference detail | =claude/context/mcp-servers.md= | Keeps MCP-specific instructions out of global memory and the long README |

When adding or changing a reusable Claude skill or hook, update the Codex
counterpart in =codex/skills/= or =codex/hooks/= in the same session.  The
=remind-ai-config-sync.sh= and =require-ai-config-sync.sh= hooks make this
visible but do not auto-edit the other side. The commit guard checks skill
content after tool-specific frontmatter normalization and only accepts
manifest-based bypasses when the manifest records an artifact-specific status or
exemption. See =../agents/README.org= for the full synchronization contract and
known non-portable cases.

*** Multi-account setup
:PROPERTIES:
:CUSTOM_ID: h:multi-account
:ID:       239E0918-2383-460C-8C9C-2ABC4E3D344D
:END:

Three OAuth accounts (personal, tlon, and epoch) are supported without re-authenticating. Each account uses a separate =CLAUDE_CONFIG_DIR= (=~/.claude-personal/=, =~/.claude-tlon/=, and =~/.claude-epoch/=) to keep its OAuth credentials and identity isolated, while sharing everything else via symlinks back to =~/.claude/=.

Shared (symlinked into each account dir):
- =settings.json=, =settings.local.json= — preferences and permissions
- =skills/= — custom skills
- =projects/= — trusted folders, project config, session logs
- =history.jsonl= — command history
- =plugins/= — installed plugins

Not symlinked:
- =CLAUDE.md= — loaded from =~/.claude/CLAUDE.md= at user level; account dirs do not have their own copy (to avoid loading the same instructions twice)

Separate per account:
- =.claude.json= — OAuth identity, user ID, and MCP servers (synced from =~/.claude.json=)
- Keychain credentials — the actual OAuth tokens

The active account is stored in =~/.claude-current-account=. From Emacs, =ai-agent-claude-start-or-switch= uses the persisted account automatically (configured via =ai-agent-claude-accounts=). Use =ai-agent-claude-select-account= to change it. From the terminal, the shell aliases =claude-personal=, =claude-tlon=, and =claude-epoch= are available.

*Adding a new account:* add it to =ai-agent-claude-accounts=, then run =M-x ai-agent-claude-init-account= *before* invoking =claude= for that account. This creates the config dir and all shared symlinks up front; otherwise =claude= writes real empty =settings.json=/=history.jsonl=/etc. on first login, which divorces the new account from your permissions and history.  If this happens anyway, re-running =init-account= (or =select-account=) heals virgin-state files automatically; non-virgin files are left alone with a warning so you can migrate them by hand.

*1Password service account:* All =op://= references resolve silently via =OP_SERVICE_ACCOUNT_TOKEN= (a 1Password service account token with read-only access to the "Automations" vault).  The token is stored in =pass= at =epoch/1password-service-account-token= and exported in =.zshenv-secrets=.  No Touch ID prompts are needed.  To rotate: delete the old service account in the admin console, create a new one with =op service-account create "Epoch Ops Automation" --vault "Automations:read_items" --raw=, and update the =pass= entry.  Item names in the Automations vault must not contain parentheses (they break =op://= parsing).

*MCP servers:* Placement rules, current inventory, multi-account behavior, and account-specific MCP secret handling live in =context/mcp-servers.md=.

**** Chrome integration and multi-account
:PROPERTIES:
:ID:       E317AE6E-6C3F-4543-A66B-3EF1D50E7719
:END:

Chrome integration uses three Chrome profiles, one per Claude Code account. Browser automation from a given Claude Code session targets the Chrome profile its account is paired to, so agents working on a tlon project automatically drive the tlon browser, and likewise for epoch and personal.

*Profile ↔ account mapping:*

| Chrome profile | Google account (Chrome)      | Anthropic identity (Claude Code) | Claude Code account |
|----------------+------------------------------+----------------------------------+---------------------|
| Default        | =pablo.stafforini@gmail.com=   | =pablo@stafforini.com=             | personal            |
| Profile 2      | =pablo@epoch.ai=               | =pablo@epoch.ai=                   | epoch               |
| Profile 3      | =pablo@tlon.team=              | =tlon.shared@gmail.com=            | tlon                |

When opening Chrome manually for browser-only service flows, use =Default= for personal projects and =Profile 2= for Epoch/work projects. For example, open Epoch Google Cloud pages with =/Applications/Google Chrome.app/Contents/MacOS/Google Chrome --profile-directory="Profile 2" URL=.

The Google column is the account each Chrome profile is signed into (independent of Anthropic). The Anthropic identity column is the account used to authenticate the Claude Code CLI inside =~/.claude-<account>/= — each =CLAUDE_CONFIG_DIR= holds its own OAuth credentials for a distinct Anthropic account, so =/usage= and rate-limit tracking report each account separately.

*Setup:* make sure each target Chrome profile has the Claude extension installed and is signed into =claude.ai= using the Anthropic identity the table above assigns to that profile. That's the whole setup — no CLI-side pairing step is needed.

*How profile selection works:* when a Claude Code session starts with =--chrome=, it opens a websocket to =wss://bridge.claudeusercontent.com=. Each Claude extension instance in Chrome also connects to that bridge. The bridge matches a CLI session to an extension instance by shared Anthropic identity, so a session authenticated as =tlon.shared@gmail.com= automatically finds the Chrome extension signed into the same account (i.e. the one in Profile 3). No per-device pair record is required.

You may see =chromeExtension.pairedDeviceId= and =pairedDeviceName= populated in some accounts' =.claude.json= (e.g. epoch's =Epoch= device). These are leftovers from an older CLI flow and are not read by current Chrome integration — they can be safely ignored or deleted. The =hooks/check-chrome-profile-pair.sh= SessionStart hook used to warn when this field was missing; it's now a no-op placeholder because the warning was based on a wrong assumption.

*Native host manifest:* there is a single manifest file shared across all Chrome profiles. Its =path= field should point to the current account's =chrome-native-host= script (e.g. =~/.claude-epoch/chrome/chrome-native-host=). In practice, all account scripts are identical (they exec the same =claude= binary with =--chrome-native-host=), so the path doesn't affect functionality today — but it should be kept in sync for correctness.

*Emacs-specific:* non-interactive sessions (Emacs) require the =--chrome= flag, set via =(setq claude-code-program-switches '("--chrome"))=. Without it, Claude Code silently skips Chrome MCP startup.

*Known issues:*
- *Silent bridge failure:* the Chrome bridge can fail to connect at session startup (e.g. due to stale sockets or timing issues). When this happens, =/chrome= shows "Status: Disabled" even though =--chrome= was passed. The only fix is starting a new session. There is no mid-session reconnect mechanism.
- *Stale bridge sockets:* old sockets in =/tmp/claude-mcp-browser-bridge-pablostafforini/= are not automatically cleaned up and may interfere with new connections.
- *Session-account binding:* if you switch accounts mid-session (e.g. via =ai-agent-claude-select-account=), Chrome will still use the original account. The running process retains its =CLAUDE_CONFIG_DIR=; you must start a fresh session under the new account — use =/handoff= to preserve context if needed.

*Architecture:*
- Chrome extension (ID =fcoeoabgfenejglbffodgkkbkcdhcgfn=) communicates via native messaging
- Native host manifest: =~/Library/Application Support/Google/Chrome/NativeMessagingHosts/com.anthropic.claude_code_browser_extension.json=
- Native host scripts: =~/.claude-{epoch,tlon}/chrome/chrome-native-host= (each execs the installed =claude= binary with =--chrome-native-host=)
- Bridge socket: =/tmp/claude-mcp-browser-bridge-pablostafforini/<pid>.sock=
- Keychain credentials per account: =Claude Code-credentials-<sha256(config_dir)[:8]>=

** Configuration
:PROPERTIES:
:ID:       48F2354B-E7A4-43D1-A5AD-856EB82225E3
:END:

*** settings.json
:PROPERTIES:
:ID:       C60E07A6-7A22-4272-8875-45FE9B44F185
:END:

Lives at =~/.claude/settings.json= as a regular file, not tracked in this repo. Claude Code writes to it directly when you run =/theme=, =/effort=, =/permissions=, toggle plugins, etc., so any attempt to symlink it back to version control breaks on the first such write. Edit it in place.

Notable current values:
- *Permission mode*: =auto= — a background classifier allows most actions while blocking risky ones.
- *Agent teams*: enabled (=CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=).
- *Effort level*: =xhigh= (persistent) with =CLAUDE_CODE_EFFORT_LEVEL=max= in =env= to force max across sessions.

*** settings.local.json
:PROPERTIES:
:ID:       709B6F3F-F5B6-44F1-AA4E-1D2BE510FC1E
:END:

Machine-specific tool whitelists and the =theme= key (synced from Emacs by =ai-agent-claude--sync-theme=). Also lives at =~/.claude/settings.local.json= as a regular file, not tracked.

*** CLAUDE.md
:PROPERTIES:
:ID:       9B404E8C-F109-4188-9F3E-1F1A9A64A37D
:END:

The global =~/.claude/CLAUDE.md= (symlinked here) is the user-level memory loaded in every project. It defines conventions that apply across all projects:

- Never present guesses as facts; state uncertainty and verify claims that matter.
- Fix root causes, not symptoms; no silent fallbacks or workaround code unless explicitly labeled, justified, and approved.
- Verify fixes end-to-end before declaring them done; say exactly what could not be verified.
- Commit each logical change immediately; prefer amending when iterating on the same logical change (unless already pushed).
- Pre-commit checklist (hook-enforced): Elisp pre-commit checks (batch-test, stage =doc/*.org=) are in the =elisp-conventions= skill; the =README.org= staging rule is in the project =CLAUDE.md=.
- Use subagents and teams liberally; prefer the most powerful model.
- Use =trash= instead of =rm -rf=; never send signals to Emacs without confirmation.
- Anything the user is asked to paste — message, credential, URL, snippet — goes to the Emacs kill ring via =emacsclient= (and =pbcopy= as well when the paste target is a Chrome or native form). Multi-line or secret content uses the temp-file =insert-file-contents= → =kill-new= pattern to avoid Elisp string-escaping.
- For =/schedule=, always set email as the notification method.
- =git push= to the user's own remotes is allowed autonomously (once the underlying commits are in); other externally-visible actions (PRs, issues, Slack, email, Asana task creation/updates) still require explicit confirmation.
- Secrets handling is imported from =context/secrets.md= with =@context/secrets.md= so the short safety rules load with the global memory.
- MCP server instructions are referenced via =context/mcp-servers.md=, not imported, so agents load them only when MCP-specific context is needed.
- Service access tools are listed by service in the global memory.

Codex does not consume this file directly. Its tracked user-level counterpart
is =codex/AGENTS.md=, with Claude-only =@context= syntax and relative
context/tool paths expressed in Codex-safe form. The live =~/.codex/AGENTS.md=
symlink points to that file, and =bin/ai-config-sync= audits semantic
equivalence after tool-specific normalization.

** Skills
:PROPERTIES:
:ID:       05017294-32AE-4465-9254-13C1BA5662CA
:END:

Skills are reusable prompt modules stored in =skills/<name>/SKILL.md=. The filename must be uppercase =SKILL.md=, since =file-expand-wildcards= is case-sensitive on the Google Drive FUSE mount. Some skills are user-invocable (triggered by typing =/<name>=), others provide context that Claude loads automatically when relevant.

Skill frontmatter descriptions should stay concise and routing-focused: include
the user phrasing, scope boundary, and important near-miss exclusions needed to
decide whether to load the skill, but keep implementation details in the skill
body.

The tracked skill bodies live under =claude/skills/= and =codex/skills/=. The
live home-directory skill roots (=~/.claude/skills= and =~/.codex/skills=) are
symlinks to those tracked trees, created by =bin/skill-prune install= or either
=claude/bin/install-skills.sh= / =codex/bin/install-skills.sh=.

Run =bin/skill-prune apply --days 60= to disable stale paired skills. The scan
checks recent Claude and Codex transcripts and disables a skill only when both
tools lack usage inside the threshold window; recent use in either tool keeps
both tools enabled. It includes global paired skills and dynamically discovered
project-local skill pairs under =~/My Drive=; override the discovery roots with
=SKILL_PRUNE_LOCAL_ROOTS=, using the platform path separator for multiple roots.
Disabled skills are recorded in =skills-disabled.json= and applied through native tool settings:
Claude =skillOverrides= entries set to ="off" in the live
=~/.claude/settings.local.json=, and Codex =[[skills.config]]= entries with
=enabled = false= in =codex/config.toml=. Re-enable a skill with
=bin/skill-prune enable NAME=; add =--keep= when a renamed or otherwise
intentionally retained skill should remain enabled despite missing history.

Epoch-only skills live in the Epoch workspace instead of this global dotfiles tree: =~/My Drive/Epoch/.claude/skills/= for Claude and =~/My Drive/Epoch/.codex/skills/= for Codex. Keep workflows there when their triggers and instructions only make sense inside the Epoch project context.

*** Context skills
:PROPERTIES:
:ID:       E1337818-A431-4C45-9239-BCEE76BEB3AE
:END:

These are not invoked directly. They supply background knowledge when Claude works in this repo.

**** dotfiles-context
:PROPERTIES:
:ID:       3AB926D5-2116-4864-93F2-B6D33CE832C5
:END:

States where dotfiles and external Emacs packages should be edited: all non-=dotfiles= elpaca packages are edited in the active profile's =~/.config/emacs-profiles/<profile>/elpaca/sources/<package>/= clone, while =dotfiles= is edited only at =~/My Drive/dotfiles/= and the elpaca clone is a read-only mirror synced by git hooks. Also covers active profile lookup, profile-aware =emacs/config.org= tangling, required Claude/Codex README updates, verification handoffs, and the commit message format (=<scope>: <description>=). See also =elisp-conventions= for Elisp testing/reload rules and =emacs/extras/doc/elisp-development-workflow.org= for the full two-clone sync architecture.

**** elisp-conventions
:PROPERTIES:
:ID:       EBC619A2-2402-43B0-8FBA-8A68EE29BB7D
:END:

Emacs Lisp coding standards: atomic functions, helper placement, docstring format. Defines the skill boundary for =.el= work, routes compiler/checkdoc lint requests to =lint-elisp= and documentation generation to =doc-elisp=, and documents the elpaca rebuild-wait-reload process enforced by the =load-elisp-after-edit= hook. Prohibits ad-hoc reload mechanisms (=load-file=, =eval-buffer=, =eval-defun=), including after shell edits that bypass edit hooks; those continue through batch-test, commit-triggered sync/rebuild, and targeted live =emacsclient -e= verification. Also documents the critical =emacs --batch= testing pattern: elpaca builds on load-path first, then =push= =emacs/extras= to the front so the edited source always loads instead of the stale =.elc= from elpaca builds. Includes pre-commit checks (batch-test and doc staging) and a transient menus section: after adding or modifying a =transient-define-prefix=, every suffix must be verified as an interactive command (transient defers validation to invocation time).

*** Development skills
:PROPERTIES:
:ID:       71770F84-ED6B-4F0B-B121-1F23CBCEB769
:END:

**** code-audit
:PROPERTIES:
:ID:       D3121B68-8480-41B2-835B-97E85BB7DE29
:END:

Audits code for actual defects: bugs, application-code security issues, error handling gaps, and correctness problems. Categorizes findings as critical, bugs, fragile, or minor, with specific line numbers and concrete failure modes. Pass =--accept= to fix confirmed in-scope findings, verify them, and commit the result.

**** design-audit
:PROPERTIES:
:ID:       F01330D3-C44B-415D-9107-C1E865FB0B63
:END:

Audits code for architectural quality: unnecessary complexity, missing or wrong abstractions, duplication, and refactoring opportunities. Pass =--accept= to auto-refactor high-confidence, behavior-preserving high-impact findings.

**** interpretability-audit
:PROPERTIES:
:ID:       800B3CAA-A993-4929-BB4A-7467A56FA70C
:END:

Audits code for reader comprehension: unclear names, magic values, missing
rationale, undocumented conventions, implicit phases, opaque score formulas,
duplicated explanatory logic, and type-safety escapes that obscure intent. Pass
=--accept= to apply high-confidence, behavior-preserving clarity fixes and
report risky renames, broad refactors, public API changes, or domain-judgment
calls instead of applying them.

**** dx-audit
:PROPERTIES:
:ID:       B2E4A6C8-1D3F-4E9A-8B5C-7F2D9A0E4C16
:END:

Audits a codebase from the AI agent's perspective: inventories existing verification infrastructure (tests, types, linting, CI), project instructions, code navigability, and automation, then produces prioritized recommendations for reducing friction. Each recommendation is concrete, justified from the AI's perspective, evidence-backed, and effort-estimated. Pass =--accept= to implement only high-confidence quick wins that are scoped, low-risk, and directly improve future agent orientation or verification.

**** symptom-check
:PROPERTIES:
:ID:       CC534E25-3A8B-495E-A6BD-7FB05B4721F3
:END:

Forces an invariant-focused root-cause check before applying any non-trivial bug
fix, after enough debugging has identified a tempting local patch. It prevents
the failure mode where agents patch the immediate symptom while the underlying
invariant stays broken, and it explicitly avoids trivial typo/formatting fixes
and greenfield feature work. Workflow: restate the symptom, consult
=architectural-issues.md= at the repository root for similar prior entries,
draft the local fix, articulate the invariant in one sentence, search for other
manifestations with code evidence, then pick a disposition (=fix locally= /
=fix locally + flag= / =stop and refactor=). The "+ flag" and "stop and
refactor" dispositions produce a registry entry for review, or add it directly
only when registry updates were explicitly authorized; the workflow then reports
the reproduction, test, lint, build, or re-read verification that was performed.

**** systematic-debugging
:PROPERTIES:
:ID:       E08A0D8A-314E-4ABD-80C2-FBBBD5E9C128
:END:

Adapted from the third-party skill previously stored under the dotfiles
project-local =.claude/skills/= tree. Forces root-cause investigation before
bug fixes, with supporting references for condition-based waiting, defense in
depth, and tracing polluters. It is now a global Claude/Codex skill so
debugging behavior is consistent outside the dotfiles project. The skill also
documents when not to trigger, how it differs from =symptom-check=, and the
evidence, root cause, fix, verification, and residual risk fields expected in
the final debugging report.

**** build
:PROPERTIES:
:ID:       15574F2D-5827-48ED-8630-DFB3843EA3FB
:END:

Spec-based build workflow inspired by [[https://x.com/trq212/status/2005315275026260309][Thariq Shaukat's method]]. Reads a one-line spec from the prompt (e.g. =/build a Spotify player for Emacs=), an explicit spec path, or a =spec.md= file in the current project, then interviews the user in depth to develop it into an implementation-ready plan. The questions are deliberately non-obvious, probing hidden complexity, unstated assumptions, UX, architecture, edge cases, testing, security, accessibility, and performance. Once the interview is complete, writes the detailed spec back to the file. Implementation proceeds only after explicit confirmation and ends with files changed, commits, verification, and unresolved issues.

**** test-suite
:PROPERTIES:
:ID:       057FB141-D07F-4BD4-B880-912B67275263
:END:

Creates or expands high-value test suites for a codebase or module. It triggers on requests to add or write tests, improve coverage, bootstrap testing, or create regression tests, while routing ordinary debugging, code review, lint cleanup, and one-off validation elsewhere. It inventories project instructions, existing test runners, fixtures, dependencies, and critical paths before writing behavior-focused tests by risk, then reports commands run, coverage added, gaps left, bugs uncovered, and unresolved failures.

**** lint-elisp
:PROPERTIES:
:CUSTOM_ID: h:lint-elisp
:ID:       92D41417-BE75-415E-BF0C-6F705EAEBE27
:END:

Fixes byte-compile warnings and checkdoc notes in one or more Elisp files. Collects diagnostics from both sources in parallel, applies fixes (forward declarations, docstring rewording, unused variable cleanup), and re-runs until the output is clean.

**** pr-audit
:PROPERTIES:
:ID:       03187E6C-B3E2-47EB-821B-68121F4F883F
:END:

PR readiness verification for the current branch. It discovers the base/PR
scope, runs project build/test checks, reviews each commit, checks upstream
freshness, scans changed files for artifacts, secrets, whitespace, and line
endings, and polls remote CI only when the branch or PR is already pushed. It
reports not-run checks with concrete reasons and does not change remote state.

**** post-push-ci
:PROPERTIES:
:ID:       D7819BB0-C383-4A3F-8950-2B9F26097E0F
:END:

Post-push GitHub Actions closeout. It applies after an agent has pushed a
commit, runs =ci-after-push= to wait for push-triggered CI on the pushed SHA,
fetches failed logs, fixes root causes, commits and pushes follow-up fixes, and
repeats until the pushed commit is green or a concrete blocker is identified.
It does not grant permission to push; it only closes the loop after an
authorized push.

*** Documentation skills
:PROPERTIES:
:ID:       7BAA2662-D24F-42B2-A865-5442B2F2E1B1
:END:

**** doc-elisp
:PROPERTIES:
:ID:       32BB20B4-C967-4FB4-9EF6-674F87BF9023
:END:

Creates or updates Org manuals for Elisp packages, following the Denote manual
style: PROPERTIES drawers, Texinfo index entries, cross-references, and
workflow-oriented prose. It defines the standalone =README.org= and dotfiles
=emacs/extras/doc/*.org= paths explicitly, distinguishes nearby
=generate-readme=, =doc-extras=, and =lint-elisp= requests, inventories public
options/faces/commands/functions across all source files, avoids broad
=.dir-locals.el= changes in the dotfiles monorepo, and requires source/manual
verification before reporting completion.

**** doc-extras
:PROPERTIES:
:ID:       C70171D9-4A5C-45BE-9F74-D7AE244336EF
:END:

Scans all =emacs/extras/*.el= files, identifies missing or outdated documentation, and generates docs in parallel using subagents.
It uses =doc-elisp= for the Denote-style manual standard, includes =paths.el=
and other top-level extras files, verifies changed Org manuals and Texinfo
exports before committing, and keeps package documentation commits scoped to one
package at a time.

**** generate-readme
:PROPERTIES:
:ID:       A3F5B8D1-7C42-4E9A-B6A1-2D8E4F9C0A73
:END:

Generates or updates a GitHub-facing =README.md= for an Emacs Lisp package from
its =README.org= manual. It verifies the package/manual prerequisites, reads the
manual, main Elisp file, and existing Markdown README, preserves accurate
README-specific material, handles package-vc/use-package =:vc= assumptions
explicitly, checks links/placeholders and roadmap moves, then commits verified
README/manual changes when the user requested file edits.

**** org-note
:PROPERTIES:
:ID:       F859475D-8BEF-46B7-9417-3F8462826BA9
:END:

Creates or updates org-mode notes in my notes directory
(=~/My Drive/notes/=), with slugified filenames, preserved existing content,
saved org IDs for new notes, and an explicit re-read verification step.

**** transcribe-note
:PROPERTIES:
:ID:       56961BB0-30BB-44D1-BC8F-D74713517D4E
:END:

Transcribes a recent or named audio file in =~/Downloads/= using whisperx, then restructures the transcript into a well-organized org-mode note saved to =~/My Drive/notes/=. Handles optional language code and filename arguments, avoids verbatim-transcript/subtitle near-misses, saves the generated org ID, and verifies the note before reporting completion.

**** add-to-emacs-packages
:PROPERTIES:
:ID:       E8FD1360-FDF6-46FE-940F-BD8A346AE4BF
:END:

Registers an Emacs package in =my-emacs-packages.org=, creates an accompanying documentation note with =#+INCLUDE= directives pointing to the package's readme, and adds a card to the =benthamite/benthamite= GitHub profile README when that externally visible update has been explicitly authorized. Determines the package name from the argument or the current directory, inserts entries in alphabetical order, saves generated org IDs for both the note and the list entry, and verifies the note/list/profile changes before committing. Marks the exported note as =:unlisted true= via =EXPORT_HUGO_CUSTOM_FRONT_MATTER= so it stays out of the site's note index. Checks both the packages list and the GitHub profile README, skipping whichever is already done.

**** convert-citations
:PROPERTIES:
:ID:       EDFDB065-9B7C-4E9A-B858-F9CCD9E1E5D2
:END:

Replaces raw bibliographic references in =.org= files with org-mode =[cite:@KEY]= citations by resolving each work against the user's citar bibliography. Takes a target file path, directory, or glob as an argument (prompted by =ai-agent-run-skill= when invoked via =ai-agent-menu= → =s=) and processes matching org files sequentially. Uses =emacsclient -e '(tlon-bibliography-lookup FIELD VALUE "=key=")'= for lookups, falling back to substring matching, DOI matching, and the current agent's web fetch/search tools for URL-based or ambiguous references. Preserves formatting (bold, italics, parentheticals, locators like =pp. 123–145= or =ch. 5=); does not fabricate keys for works not found in the bibliography; and verifies edited files for Markdown citation tags or placeholder markers before reporting. Canonical style reference: =~/My Drive/notes/pablos-miscellany/c-d-broad-a-bibliography.org=. This is a native agent replacement for =tlon-bib-replace-citations-in-file=, whose gptel-based agentic loop was brittle for this kind of lookup-heavy task.

*** Release and operations skills
:PROPERTIES:
:ID:       F49B76EA-C29B-45D6-AD8D-D3E47DB4073E
:END:

**** release
:PROPERTIES:
:ID:       10DF729B-32D5-48E0-A150-9F5D18F799AF
:END:

Cuts releases for my Emacs packages (read dynamically from the listed package headings in =my-emacs-packages.org=, excluding the unlisted section). Validates the repo, classifies commits since the last tag, suggests a semver bump, and drafts release notes. Also has an audit mode that scans all packages for version mismatches or unreleased commits without cloning or creating tags/releases. Handles untagged package repos as initial releases, strips flags such as =--accept= before package inference, and routes dotfiles releases to =release-dotfiles=. Pass =--accept= to skip the confirmation gate.

**** release-dotfiles
:PROPERTIES:
:ID:       49102EA7-4B49-4F93-8134-06FA7925AE55
:END:

Cuts releases for the dotfiles repo itself. Verifies that every personal package repo has a clean working tree in its local elpaca clone, checks the dotfiles working tree, classifies commits since the last semver tag, suggests a semver bump, drafts release notes, and creates a GitHub release. Clean unpushed package commits are pushed only after explicit authorization or =--accept=, untracked =.elc= cleanup is selected with =git ls-files= and moved via Trash, and =--accept= never skips the post-lockfile profile-test gate.

**** triage-issues
:PROPERTIES:
:ID:       D1A897E8-29DC-4FFD-8D16-2AC792A4DA0F
:END:

Scans, prioritizes, and batch-resolves open GitHub issues for =tlon-team/tlon.el=. Starts with a scope and mode gate that verifies the current checkout, separates read-only triage, local fixes, and externally visible closure, and requires explicit authorization before GitHub comments, issue closure, or org archiving. It presents batches before edits, uses subagents or agent teams when available, verifies =TLON_AI_GITHUB_TOKEN= belongs to =tlon-ai= without creating test comments, commits fixes issue-by-issue, and reports commit, check, GitHub, and org-archive verification.

**** gsc-indexing-triage
:PROPERTIES:
:ID:       B4CAB9FD-AFE7-4E5F-A7D6-582C76D68F73
:END:

End-to-end workflow for Google Search Console Page indexing emails and validation failures for =stafforini.com=. Reads personal Gmail alerts from =sc-noreply@google.com= via =claude/bin/gmail.py=, decodes the "View issue details" =c.gle= links with the bundled =scripts/extract-gsc-links.py= helper resolved through =bin/agent-skill=, checks current Search Console examples and the persistent log at =stafforini.com/logs/gsc-indexing.md=, fixes root causes in the proper source repo rather than generated =content/=, and runs the stafforini.com export/test/render verification path. It starts with a scope gate that separates read-only triage, local fixes, and externally visible actions, so quick deploys, Search Console validation, and Gmail archiving only happen when the current request explicitly authorizes them. It appends a durable log entry with messages, issue URLs, examples, commits, verification, deploy and validation status, archived emails, and follow-up. Its frontmatter is kept Codex-safe because Codex skips skills whose =description= field is not parsed as a string.

**** update-log
:PROPERTIES:
:ID:       DCC9EC24-1AB4-495E-8C89-DB4C63229744
:END:

End-of-session bookkeeping triggered by =/update-log=, "update log", "session
log", "close out", "wrap up", or direct requests to save end-of-session
progress. Treats =CLAUDE.md= as the canonical session-log index unless the
project says otherwise, detects the project's log directory from the
latest-session pointer, snapshots the working tree before edits, and creates a
dated session log (what was done, key findings, results, open questions). On
first run in a new project, gathers the log directory and optional =decisions/=
setup with =AskUserQuestion= when available or a concise fallback question
otherwise. Updates the =CLAUDE.md= session reference, uses the
=record-decisions= skill or its local workflow if applicable, runs and reports
=context/post-update-log-hook.md= hooks, and commits only intended bookkeeping
paths. For Epoch project notes, the hook guidance treats the main project
=.org= file as a concise ground-truth brief: refresh current-state fields,
track active work as org =TODO= headings rather than checkbox mirrors, archive
completed =DONE= headings and stale historical narrative to
=<project>_archive.org=, and avoid routine =** Meeting references= sections
unless a meeting link supports a durable decision or constraint. Before
committing, verifies every intended path was actually staged (=git
diff --cached --name-only= vs the intended list, with =git check-ignore= for any
missing path), checks the cached diff so pre-existing dirty hunks are not swept
into the commit, and stops with a clear report rather than silently producing a
partial commit when a parent repo's =.gitignore= has filtered the log. Reports
the log path, pointer, hook/decision status, commit hash, and verification;
accepts =--exit= to end the session via the host environment's normal exit
mechanism after completing all steps.

**** record-decisions
:PROPERTIES:
:ID:       D62A0EFB-05AC-4A56-88A0-668E632460AE
:END:

Extracts and records architectural, algorithmic, or design decisions from the current session into =decisions/NNN.md= files. Called automatically by =/update-log= or invocable on demand when the user asks to record/capture an ADR or decision. It records choices with explicit alternatives, plus user-requested records without inventing rejected alternatives; reads existing decision context before writing; creates a missing =decisions-summary.md= only when =decisions/= already exists; updates summary rows with status values; and verifies every edited decision file and summary row before reporting.

**** open-session-log
:PROPERTIES:
:CUSTOM_ID: h:open-session-log
:ID:       1C8D18F2-3EA7-4149-9BC4-40B4EFE8546C
:END:

Opens the current Claude Code session's conversation log in Emacs using the =agent-log= package. Walks up the process tree to find the session ID, then calls =agent-log-open-session= via =emacsclient=. Avoids latest-session fallbacks because concurrent Claude Code sessions can make recency ambiguous.

**** move-session-log
:PROPERTIES:
:ID:       98D5F4FA-F05B-423A-8A16-967234CA1FDC
:END:

Moves a Claude Code internal session log (=.jsonl= and associated directory) from another project's =~/.claude/projects/= directory to the current project's. Also rewrites the =project= field in =history.jsonl= and =cwd= fields in the session =.jsonl= so that =agent-log-resume-session= opens the session in the correct directory. Useful when a Claude session was started in the wrong project context. Accepts a session ID as argument; if none given, lists the 10 most recent Claude sessions from other projects and asks the user to pick one. This skill is Claude-specific; Codex sessions live under =~/.codex/sessions= and use the Codex-side =move-session-log= skill.

**** verify
:PROPERTIES:
:CUSTOM_ID: h:verify
:ID:       D25CCDE3-F976-493E-9DD8-043A17422C65
:END:

Executes a task under an explicit verification loop when the user asks to
verify, close the loop, define success criteria before work, or rigorously
check an output whose correctness is not automatically answerable. It generates
concrete criteria before acting, meta-verifies criteria quality, proceeds
without a user checkpoint when criteria are clear, asks only when criteria
require user/domain judgment or change scope/risk, evaluates each criterion,
loops on failures, and reports pass/fail/uncertain evidence. Ordinary coding
and debugging use narrower project checks unless this meta-verification
workflow was explicitly requested.

**** handoff
:PROPERTIES:
:ID:       CAB83D10-E108-48D4-871B-17821B1D3CD6
:END:

Saves next-session instructions to =/tmp/claude-code-handoff.md= for seamless
Claude Code session transitions. Two paths: (1) if the user explicitly gave
next steps, writes a faithful prompt directly; (2) if no explicit next steps
exist, infers them from conversation history, prints the proposed prompt as a
code block, and asks the user to confirm via =AskUserQuestion= before writing.
Inferred prompts include exact dates, concrete file paths, command names,
commit hashes, and verification state so the next session does not depend on
chat history. The skill re-reads the temp file after writing, prints the saved
contents, and tells the user to run =! emacsclient -e '(ai-agent-claude-handoff)'=;
Claude must not run that command itself.

**** tangodb-continue
:PROPERTIES:
:ID:       E913CE0D-4D9C-42E6-82A5-97BA231521A3
:END:

Tangodb session-start and resume workflow. It is only for starting or resuming
tangodb cleanup work; ending sessions belongs to =tangodb-closeout=, and
generic handoffs belong to the general handoff workflow. Reads the applicable
project guide files, the data-cleanup plan, =docs/data-cleanup-progress.md=,
and required slice docs, then resumes the exact next step from the progress
file. It keeps chaining safe read-only slices until a progress-file hard stop,
approval gate, externally visible action, or real blocker is reached, and
reports the completed slice, verification, stop reason, and closeout state.

**** tangodb-closeout
:PROPERTIES:
:ID:       4CC505DA-FBF3-4111-9397-D1FB3D936981
:END:

Tangodb end-of-session closeout workflow. It is only for ending tangodb work;
starting or resuming belongs to =tangodb-continue=, and generic handoffs belong
to the general handoff workflow. Updates or creates
=docs/data-cleanup-progress.md= with the active plan, current state, exact next
step for =continue=, required first reads, hard stop rules, verification,
blockers, and compact closeout history so a later Claude Code or Codex session
can resume without chat history. Before final response, it re-reads the progress
file, checks the progress-file diff, and runs =git diff --check=.

**** emacs-freeze
:PROPERTIES:
:ID:       5ED7EB34-774B-489C-95C5-27C8131CE048
:END:

Diagnoses a frozen, hung, beachballing, or otherwise unresponsive Emacs process. First tries to dump =*Backtrace*= and =*Messages*= via the server socket using per-PID diagnostic files and a macOS-safe timeout helper, then samples the stack as fallback evidence. Analysis forces a stuck-vs-busy check and a nested-debugger count *before* identifying any hot path, to avoid confusing timers firing inside a wedged recursive edit with the actual cause. Recovery options are tailored to the diagnosis: =(top-level)= via emacsclient for debugger nesting, =keyboard-quit= for responsive-but-stuck, SIGUSR2/SIGTERM/SIGKILL (with warnings) otherwise; every state-changing recovery action requires explicit confirmation before the agent runs it. Cleanup uses =trash= or non-recursive removal of the known diagnostic files. Explicit rules forbid unverified citations and require labelling observation vs. inference vs. unknowns.

**** nosync
:PROPERTIES:
:ID:       BEEADA41-ECD4-457B-8A10-DA8D0C19984B
:END:

Moves one explicitly named generated directory from inside =~/My Drive/= to
=~/.drive-nosync/= (outside Drive's sync root) and leaves a symlink in its
place. Drive skips symlinks, so the directory becomes invisible to sync while
tooling works transparently. The workflow refuses to merge or overwrite an
existing mirrored target, verifies the replacement symlink, and walks the
relocated tree to rewrite broken relative symlinks whose =..= chain pointed
outside the moved directory. Remaining broken symlinks are reported instead of
hidden. Use when Google Drive complains about a specific directory (e.g.
=node_modules/.bin=); use =fix-drive-errors= for broad scans.

**** fix-drive-errors
:PROPERTIES:
:ID:       69244292-277A-4484-BC21-2AA9C8D588D9
:END:

Bulk version of =nosync=. Scans =~/My Drive/= for generated directories causing sync errors (node_modules, build output, Python caches, virtual environments), skips ambiguous source/user data, moves confirmed offenders to =~/.drive-nosync/=, rewrites broken relative symlinks after each move, reports unresolved conflicts or missing targets, and restarts Google Drive after changes. Use periodically or when the error list grows.

**** install-mcp-server
:PROPERTIES:
:ID:       AAB6CC73-A936-4D25-9255-544436259405
:END:

Installs and configures a new MCP server for Claude Code. Walks through discovery, auth verification (checking for platform-locked OAuth before installing), dependency installation, credential-safe setup, and registration via =claude mcp add=. Encodes hard-won lessons: always add a trailing slash to HTTP/SSE URLs (servers without it are silently invisible), verify with =claude mcp list= (not =claude mcp get=, which reads config but doesn't check runtime health), use absolute interpreter paths (=/opt/homebrew/bin/node=, not bare =node= which is an nvm shell function), use =-s user= for global servers and run the absolute =~/My Drive/dotfiles/claude/bin/sync-mcp-servers.sh= sync script afterward, use =-s project= for shared =.mcp.json= project servers, avoid the default =-s local= unless explicitly requested, prepare secret references before registration, and tell users to restart running Claude Code sessions before expecting new MCP tools to appear.

*** Multi-model and research skills
:PROPERTIES:
:ID:       8BF704E6-F252-4E27-846A-0B2F9C52B62C
:END:

**** best-of-n
:PROPERTIES:
:ID:       76E6E656-8DCE-4433-B9AA-1F6D0ACDB3B0
:END:

Adapted from [[https://pjh.is/][Peter Hartree]]'s [[https://github.com/HartreeWorks/skills][HartreeWorks/skills]] repository. Queries multiple AI models with temperature-varied sampling. For each model, generates N responses and selects the best one (using Gemini 3 Flash as judge). Then synthesizes across models using Claude Opus 4.6 with extended thinking. Has a brainstorm mode that merges unique ideas instead of picking one winner. Presets: Quick (4 models x 3), Intense (7 x 5), Ultra (7 x 6). The skill resolves the live Claude/Codex skill directory before running, uses =models.json= as the source of truth for preset details, excludes deep-research/browser presets from the normal menu, and verifies result files before reporting completion.

**** proofread
:PROPERTIES:
:ID:       D9EC1B04-5470-4EF9-8C4C-FEDFB2333128
:END:

Adapted from [[https://pjh.is/][Peter Hartree]]'s [[https://github.com/HartreeWorks/skills][HartreeWorks/skills]] repository. Proofreads Markdown files using either aspell (fast, spelling-only) or an LLM (Gemini Flash, with configurable depth: mechanical, light style, or comprehensive). Resolves the live tool-specific skill directory, keeps API keys scoped to the skill =.env= file, writes reviewed output to =*.proofread.md=, and applies accepted suggestions to =*.final.md=. Uses British English conventions by default. Depends on =ai= (v6) and =@ai-sdk/google= (v3).

*** Twitter and communication skills
:PROPERTIES:
:ID:       6E879560-81C4-4842-90F2-AC14D8E0A1CC
:END:

**** google-sheets-comments
:PROPERTIES:
:ID:       EFE90D9A-B6DB-47BA-BCED-58415B71F4EA
:END:

Handles Google Sheets comment review, replies, resolves, and reopens using
Drive comments as the source of truth. Prevents agents from treating Gmail
notification emails as canonical comment state, assuming email replies posted
to the Sheet, or triggering for ordinary spreadsheet editing. The workflow
lists comments with =gdoc comments --account <account> --json --all=, processes
one Drive comment/reply at a time, quotes the exact comment, waits for explicit
approval before any externally visible action, uses the matching =gdoc reply=,
=gdoc resolve=, or =gdoc reopen= command, and verifies with =gdoc comment-info=
before moving on.

**** twitter
:PROPERTIES:
:ID:       F2720AF8-171F-4DF0-BA03-B16E2F923C2E
:END:

Reads and searches Twitter/X content via the active skill copy's =lib/twitterapi.sh= curl wrapper around the twitterapi.io REST API. One subcommand per call type: =tweet=, =replies=, =tweets=, =user=, =search=, =followers=, =following=, =users=. Replaced the =twitterapi-io= MCP server (no more tool schemas loaded into context per session). Supports single tweet lookup, thread/reply retrieval, user timelines, keyword search, user profile lookup, follower/following checks, and saving tweets as markdown files with engagement stats and canonical URLs. The skill documents explicit Claude/Codex wrapper paths, API-key logging safeguards, cursor-capable subcommands, pricing and rate-limit boundaries, and response/save verification. Cross-references =twitter-digest=, =twitter-discover=, and =twitter-vet= for higher-level workflows.

**** twitter-digest
:PROPERTIES:
:ID:       F677671C-6C97-4BC6-B2F5-A4E0E2457C92
:END:

Fetches recent tweets from a named list of accounts, triages them against the list's description (used as a relevance rubric), and surfaces only notable content. Uses a shell script (=fetch-tweets.sh=) requiring the =TWITTERAPI_API_KEY= env var, which curls the twitterapi.io REST API directly and extracts only the fields needed for triage (~130 chars/tweet), bypassing MCP tools whose raw JSON responses (~120K chars/account) would consume enormous context. The paired Claude/Codex copies set active skill paths before fetching and saving digests, while keeping curated list files synchronized in both skill trees and using the shared Claude-side =twitter-vet= registry. =fetch-tweets.sh= validates usage, list names, and list existence before credential resolution, resolves credentials without printing secrets, and fails loudly if no listed account fetch succeeds. Digests are saved to the active tool's =digests/= directory for archival. Discovered accounts (surfaced via retweets) are checked against the vet registry and excluded if previously rejected or below threshold; remaining accounts are vetted through the read-only =twitterapi.sh= wrapper using the shared =twitter-vet= procedure (including the epistemic quality gate) and auto-added to both paired list files if they score >= 7. The workflow verifies fetch markers, saved digest, last-run timestamp, diff cleanliness, and staged paths before committing changed digest, timestamp, list, and vet registry files. Pairs with =/loop= for periodic digests.

**** twitter-vet
:PROPERTIES:
:ID:       A7B3E1D4-5F92-4C8A-B6E0-9D2F4A1C7E53
:END:

Shared account vetting procedure used by =twitter-digest= and =twitter-discover=, and directly for requests to vet, score, rank, or decide whether a Twitter/X account belongs on a curated list. Defines required inputs (handle, list name, rubric, optional =vet-threshold=), a free quick filter, and a full scoring pass through the active =twitter= skill wrapper rather than a hardcoded Claude-only path. Includes a universal *epistemic quality gate* that penalizes engagement-optimized framing, overclaiming, and hype, and rewards calibrated confidence, caveats, and uncertainty acknowledgment. The shared vet registry remains under =~/.claude/skills/twitter-vet/vetted/= so Claude and Codex callers do not split history. Existing digest lists auto-add accounts scoring above threshold; discovery callers receive scores without premature list edits until their save phase. Standalone runs verify registry/list placement, API sample counts, and clean diffs before committing changed list and registry files.

**** twitter-discover
:PROPERTIES:
:ID:       D3FF4B1E-429E-4999-951C-FBFB30F1E488
:END:

Iterative, graph-based discovery of high-value Twitter accounts in any niche. Interviews the user for criteria, acquires seed accounts via search, then expands using a "hot promise score" algorithm: accounts followed by multiple high-scoring seeds are profiled next, with scores recomputed after each round. Uses the active Claude/Codex skill root for the Twitter API wrapper, =twitter-vet= rubric, result logs, digest lists, and vet registry; keeps API responses in a per-session temporary directory; verifies the helper script before parsing; and re-reads saved logs/lists/registries before reporting completion. Presents tiered results (must-follow, strong, situational) at checkpoints and optionally exports to a =twitter-digest= list.

**** ai-journal
:PROPERTIES:
:ID:       47E2C34F-6874-4D38-8567-8B48C94836BA
:END:

Scans Claude Code and Codex session transcripts since the last run, surfaces interesting AI use cases the user may want to write about (novel prompting techniques, clever skill combinations, debugging surprises, meta-observations, automations built), presents them as numbered candidates for selection, and drafts informal study-diary posts for publication at stafforini.com/ai. Uses a Python script (=find-sessions.py=) to list sessions modified after the cutoff from both =~/.claude/projects= and =~/.codex/sessions=, deduplicating by resolved path (since =~/.claude-epoch/projects= is a symlink to the same tree). The cutoff is stored in the paired =last-run.txt= files under both =claude/skills/ai-journal/= and =codex/skills/ai-journal/=; the workflow checks that they match before scanning and updates them to the same timestamp at the end of each successful run. On first run, the skill asks the user how far back to scan. Drafts are written as *org source files* under =~/My Drive/notes/ai-journal/<slug>.org= with =:EXPORT_HUGO_SECTION: ai= on the heading, and the normal stafforini.com notes-export pipeline (=scripts/export-notes.sh=) renders them to =content/ai/<slug>.md=. The skill never writes markdown under =content/= directly — that directory is gitignored and generated by ox-hugo. The org sources are committed in the notes git repo; generated markdown is rebuilt on each deploy.

*** Personal skills
:PROPERTIES:
:ID:       B7A2F3E1-8C4D-4A5B-9E6F-1D2C3B4A5E6F
:END:

**** finances
:PROPERTIES:
:ID:       9FB8F226-1770-4571-B164-5575729722A7
:END:

Imports CSV bank statements from Wise and Mercado Pago, categorizes transactions against a guide built from ~7,500 historical ledger entries, and generates ledger-cli entries via a staging workflow. Three modes: =import= (default) parses CSVs, categorizes, writes a staging file for review, merges approved entries into =journal.ledger=, reconciles, and commits; =reconcile= compares existing journal entries against statements and marks matched entries as cleared; =update-guide= scans recent journal entries for new payee-to-category mappings. The paired categorization guide (=categorization-guide.md=) classifies payees into deterministic (auto-categorize), high-confidence (use default unless description suggests otherwise), and ambiguous (always ask), and guide edits are kept synchronized under =claude/skills/finances/= and =codex/skills/finances/=. Cash transactions must be entered manually. Statement CSVs go in =~/My Drive/ledger/statements/{wise,mercadopago}/=; shell commands in the skill use =$HOME/My Drive= paths, validate with =ledger -f journal.ledger bal=, and move discarded staging files to Trash.

*** Security skills
:PROPERTIES:
:ID:       503D6D12-1566-44FB-8BFA-408034BBB7D2
:END:

**** audit-mac-app
:PROPERTIES:
:ID:       0266B51A-276F-40DB-85F9-DDFBDF19E71D
:END:

Adapted from [[https://pjh.is/][Peter Hartree]]'s [[https://github.com/HartreeWorks/skills][HartreeWorks/skills]] repository. Static-first security audit of macOS applications before running them or granting sensitive permissions. Covers trust verification, permission analysis, app type detection, Electron source extraction and security checks, network endpoint scanning, and spaces-safe binary analysis. The bundled scanner uses temporary directories created with =mktemp= and moves extracted sources to Trash when possible. Final reports include the verdict, highest-severity findings, verification performed, and static-analysis limitations.

**** gitguardian-triage
:PROPERTIES:
:ID:       A3ED4593-1323-4991-8D41-2AA9AF45D093
:END:

End-to-end processor for the GitGuardian secrets dashboard. Fetches all open incidents via the GG API (token lives in =pass auth-sources/api.gitguardian.com/pablo@stafforini.com=), classifies each into a disposition bucket (rotate, already-invalid, false positive, low risk, test credential, or needs-user-input for unknown-validity), and drives rotation end-to-end for the valid ones. Rotation is routed to the Chrome profile that owns each credential per the mapping in [[*Chrome integration and multi-account][Chrome integration]]: GitHub/OpenAI/Anthropic/Google/Telegram each have provider-specific steps (Regenerate PAT with scope matching, legacy-user-key caveats in OpenAI, =gcloud alpha services api-keys= for Google Cloud, =/revoke= via BotFather in Telegram Desktop). Extracting new secrets from browser tabs uses a download-attribute trick (macOS blocks clipboard writes from backgrounded Chrome tabs), then passes tokens through chmod-600 files, stdin, env vars, or =pass= without printing them. Consumer enumeration runs five complementary searches per leaked value: =.zshenv-secrets=, every line of every =pass= entry (NOT just =head -1= — multi-field entries hide the leaked value on a =<subkey>:= line that =auth-source-pass-get= reaches by name), Elisp/code references to matching pass-entry path prefixes (catches =(auth-source-pass-get "gptel" (concat "tlon/core/openai.com/" tlon-email-shared))= where the literal never appears in source), direct file-level grep for the literal, and indirection layers (=op://= URIs, =gh= keychain). After rotation, the skill re-runs the searches against the OLD value to verify zero remaining consumers. Updates =.zshenv-secrets= and =pass= consumers, commits the dotfiles change, cleans up sensitive temporary files, and closes each GG incident via =/resolve= (rotated) or =/ignore= with =false_positive= | =low_risk= | =test_credential= (the only three valid reasons). Documents the rule "look for the variable, not the exact value" when finding consumers — rotated keys can share git history with their dead predecessors. Explicitly does not rewrite git history; rotating + marking resolved is enough once the old value is dead. After rotating a GitHub PAT, the skill also refreshes =gh='s macOS-keychain entry via =env -u GH_TOKEN gh auth login --with-token < "$NEW_TOKEN_FILE"=, otherwise launchd jobs (which don't inherit =GH_TOKEN=) fall back to the stale keychain token and HTTPS pushes break silently.

**** security-audit
:PROPERTIES:
:ID:       8D6548E8-93F8-40B8-9619-DF1956653ED7
:END:

Audits the development environment security posture across four domains, each runnable independently via flags. Framed around the threat model "you will eventually run malware via supply chain compromise; limit what it can do." =--secrets=: plaintext credentials in configs, globally exported secrets in shell rc files (recommends =envchain= / per-process injection), git-crypt coverage, gitignore gaps, shell history leaks, 1Password/pass consistency, =.envrc= keychain lookups. =--deps=: =ignore-scripts= in =.npmrc= (postinstall as primary supply chain vector), =minimum-release-age= (native in npm 11.10+, pnpm 10.16+), known vulnerabilities via =npm audit= / =pip audit= / =cargo audit= / Dependabot, unpinned versions, =--frozen-lockfile= usage, Python =setup.py= install-time risk, Socket.dev for GitHub repos. =--machine=: macOS Lockdown Mode, code execution isolation (VMs, Claude Code Web), browser profile separation, password manager presence, Google Advanced Protection, hardware security keys, plus existing checks (FileVault, Firewall, Gatekeeper, SIP, SSH keys, browser extensions, login items, egress controls). =--claude=: permission mode compensating controls, MCP server credential storage, hook coverage gaps, unenforceable CLAUDE.md safety instructions, skill privilege escalation risk, memory exposure. Produces findings organized by severity (critical, high, medium, low) with specific file paths and concrete remediation steps. Secret scans must redact raw matches during collection, not only in the final report, and external remediation is opt-in. Complements =block-secret-leak.sh= (which prevents leaks in real time) by auditing the broader security posture on demand.

*** Meta skills
:PROPERTIES:
:ID:       57A97EB8-103B-4D7E-9BB2-BA9AE81C177C
:END:

**** migrate-profile
:PROPERTIES:
:ID:       025A523A-F1AE-43F8-89DB-13FF2A5A98B1
:END:

Consolidates Claude Code project data (sessions, memory, trust entries) across elpaca profile changes. Runs a dry run and asks for confirmation before retargeting the =~/.config/emacs-profiles/active= symlink, copying data, rewriting JSON/JSONL paths, or trashing old source directories. Scans =~/.claude/projects/= for all directories matching the elpaca pattern, groups them by package name (extracted by splitting on =-elpaca-repos-= or =-elpaca-sources-=), and merges old-profile entries into the current profile's directory without overwriting existing target data. Also migrates project authorization entries in =~/.claude.json= so Claude Code doesn't re-prompt trust dialogs, and rewrites =project= paths in =history.jsonl= and =cwd= fields inside session =.jsonl= files so that =agent-log-resume-session= opens sessions in the current profile directory. Handles package renames by keeping the old source package name separate from the new target path during rewrites. No explicit package list is needed.

**** automate
:PROPERTIES:
:ID:       A1AC6915-C52E-4382-B1A6-731C697E736B
:END:

Meta-skill for reusable AI workflow design. Given a description of something to automate, analyzes the requirements along dimensions like context source, tool access, multi-step reasoning, and Emacs integration, then recommends the right approach: a Claude Code skill, a Codex skill, a gptel directive or preset, a gptel tool, or a gptel command. After confirmation, builds it in the canonical dotfiles paths, verifies the result, and commits the logical change.

**** skill-discovery
:PROPERTIES:
:ID:       8987E24A-517D-469D-AC77-0984631386B6
:END:

Scans Claude Code and Codex session transcripts since the last run, discovers manual workflows that repeat across two or more sessions (same multi-step procedure, same context re-supplied to the agent, same class of bug or request resolved the same way), and presents them as candidates for new skills or extensions of existing ones. Reuses =ai-journal='s =find-sessions.py= scan script, tracks its own cutoff in paired =last-run.txt= files under both =claude/skills/skill-discovery/= and =codex/skills/skill-discovery/=, and dispatches selected candidates to =automate= rather than building new skills itself — keeping discovery and build concerns separate. Before proposing a new skill, checks both the Claude and Codex skill trees for an existing skill that already covers the pattern, framing such matches as a gap to extend rather than a duplicate to create. Pairs with =/schedule= to run on a weekly cadence with email notifications (Ole Lehmann's "audit your week" pattern, [[https://x.com/itsolelehmann/status/2044425992802848954][origin tweet]]).

**** skill-creator
:PROPERTIES:
:ID:       A0C1015B-51E0-4388-9CE2-FDE1B8DC1282
:END:

Third-party skill creation workflow previously stored under dotfiles
project-local =.claude/skills/=. Helps create and improve skills through a
draft -> test -> review -> revise loop, distinguishes authoring/eval work from
one-pass audits, and keeps paired Claude/Codex copies synchronized. Uses
realistic eval prompts, =expectations= schema fields, configuration/run
directories such as =with_skill/run-1/= and =old_skill/run-1/=, benchmark
artifacts, and the bundled HTML review viewer. It can also run trigger
description optimization after the skill body is stable.

**** skill-audit
:PROPERTIES:
:ID:       5865021A-BC02-4C3B-9A73-8D58682B67B0
:END:

Audits Claude/Codex skills and =SKILL.md= files for trigger quality, workflow
design, progressive disclosure, bundled resources, verification, portability,
and maintainability. Focuses on whether the skill captures a repeatable
workflow, triggers on realistic user phrasing, gives ordered operational
guidance, keeps trigger descriptions concise without losing routing-critical
phrases or frontmatter safety, uses references/scripts appropriately, defines
output and verification expectations, and avoids surprising side effects. It
distinguishes skill audits from neighboring code, design, security, and
documentation audits, compares paired Claude/Codex copies when applicable, and
in =--accept= mode applies only high-confidence fixes that preserve the skill's
intended scope before verifying and committing them.

**** find-skills
:PROPERTIES:
:ID:       8565265E-E80C-4483-A09C-F38FDD2243D3
:END:

Third-party discovery helper for the open skills ecosystem. Uses the =npx
skills= CLI and =skills.sh= catalog to search for installable skills when a
requested capability may already exist. Requires current catalog/source
verification before recommending a skill and explicit approval before installing
an exact package. Moved from dotfiles project-local =.claude/skills/= into the
global Claude/Codex skill trees.

**** skill-resolver
:PROPERTIES:
:ID:       9F4D94C6-19C7-4DE2-9A5B-98C52064495C
:END:

Resolves Claude/Codex skills by name through =bin/agent-skill= instead of
relying on manual root-alias expansion. Searches project-local roots from the
current or supplied =--cwd= before workspace/global/system/plugin roots, exposes
=path=, =cat=, =list=, and =roots= commands, and tells agents to edit canonical
tracked skill paths when paired copies need changes.

**** diagnose
:PROPERTIES:
:CUSTOM_ID: h:diagnose
:ID:       D1D774DB-B9D0-46A3-827D-D217AA28D58C
:END:

Diagnostic mode modifier. Prefix any question with =/diagnose=, ask why something went wrong, request root-cause analysis, or ask to investigate process/tooling friction without immediately changing code. Treats errors and confusion as symptoms to diagnose before patching. The deliverable is a diagnosis, not a patch: the agent checks which existing hooks, skills, configs, or scripts should have prevented or detected the failure; verifies whether each fired and whether its output was acted on; identifies the root cause category; considers whether the issue is part of a broader pattern; and reports findings before implementing remedies. Includes explicit criteria for what counts as a structural fix (a concrete mechanism like a hook, config change, or deterministic resolver CLI) versus aspirational statements that depend on memory and discipline. Requires the usual check before proposing any fix — /would an existing mechanism, firing as intended, have prevented this immediate failure?/ — then adds a second check for whether that mechanism is actually enforced or merely prose that asks the agent to perform a brittle deterministic operation by attention. The skill can therefore distinguish plain behavioral lapses from behavioral lapses with a structural remedy.

**** optimize-claude-md
:PROPERTIES:
:ID:       6ACB6F22-2E3E-4BDD-B107-06CC109282EA
:END:

Optimizes a project's Claude Code instruction files for maximum agent instruction-following while minimizing context waste. Evaluates every instruction against eight criteria (universal applicability, specificity, pointer-vs-copy, linter delegation, default behavior, conflicts, operational-vs-reference placement, positioning), checks WHAT/WHY/HOW coverage and progressive disclosure, and produces a rewritten file with dead weight removed, non-universal rules externalized to =.claude/rules/=, imports, or skills, and critical instructions positioned at top and bottom. It treats local memories as private conflict context, handles =--accept= as an applied mode with verification, and keeps risky or broad rewrites as reported follow-up instead of half-applying them. Grounded in Anthropic's official guidance (~200 line target, concrete verifiable rules) and the HumanLayer research on instruction budgets (~150-200 effective instructions, with ~50 consumed by the system prompt). Pass =--accept= to apply high-confidence changes without prompting.

**** claude-config-audit
:PROPERTIES:
:CUSTOM_ID: h:claude-config-audit
:ID:       EEA4D06C-0E70-4A12-A588-81114465D2A4
:END:

Audits the entire Claude Code configuration for redundancy, conflicts, dead weight, staleness, and instruction drift. Discovers all instruction sources (CLAUDE.md files, context skills, memory, hooks, settings, and =claude/README.org= for cross-referencing), redacts sensitive settings values, extracts every discrete rule, and evaluates each on five criteria: whether it restates default behavior, conflicts with another rule, duplicates something elsewhere, was a reactive fix for one bad output, or is too vague to apply consistently. Produces a cut list, conflict list, and cleaned-up CLAUDE.md; pass =--accept= or explicitly ask to apply cleanup to make high-confidence scope-preserving edits, verify changed files and sync checks, and commit the result.

**** try-hartree-skills
:PROPERTIES:
:ID:       C9B601B2-393A-4098-B04B-82630F737520
:END:

Evaluates third-party skills from the [[https://github.com/HartreeWorks/skills][HartreeWorks/skills]] repository, one at a time. (The repository is maintained by my friend [[https://pjh.is/][Peter Hartree]], a Claude Code power user.) It is an interactive queue, not a general installer: the agent tries one listed skill per turn, uses the current tool's tracked skill directory, mirrors kept global skills across the paired Claude/Codex trees, tests with a realistic low-risk prompt, waits for the user's verdict, moves rejected candidates to Trash, and records =keep=, =removed=, =revisit=, or =skipped= with a note.

**** walk-list
:PROPERTIES:
:ID:       01E6C587-5B48-4478-B002-BA5388C32A5B
:END:

Accepts JSON arrays, newline-delimited JSON, and newline-separated text files.
It is for strict item isolation and controlled concurrency, not ordinary
summarizing, filtering, sorting, or bulk transformation of lists that can be
safely read all at once. Generated stub commands use the same =walk.py= path
that started the walk, and the script validates positive concurrency limits and
non-negative stale-claim thresholds.

Processes items from a list file strictly one at a time (sequential) or N-at-a-time (parallel subagent pool), guaranteeing no item is skipped or batched. =walk.py start= /moves/ the input file into =~/.claude/walk-list-data/<uuid>/= and leaves a stub at the original path; a PreToolUse hook (=block-walk-list-access.sh=) denies every tool (Read, Grep, Glob, Edit, Write, Bash, etc.) that tries to touch the protected store except =python .../walk.py= itself — the main session cannot peek ahead, and subagents only see the single item handed to them in their prompt. State updates go through =flock= so concurrent =record= calls from pool subagents cannot clobber each other. Subcommands: =start=, =next= (sequential), =dispatch= + =record= (pool), =pool-status=, =status=, =show-decisions=, =release-stale= (reclaim stuck slots), =set-max-concurrent=, =restore= (finalize and write decisions alongside the original file), =abort=.

** Hooks
:PROPERTIES:
:ID:       4D862953-7E8A-4154-BC16-919ED6B092A5
:END:

Twenty-eight hook and utility scripts handle workflow invariants, security, Emacs integration, and Claude/Codex configuration sync. Most Claude hooks are defined in the global =~/.claude/settings.json= and use early-exit guards to skip inapplicable projects. Codex has a separate global registration in =codex/hooks.json=; dotfiles-specific Codex behavior is implemented by registered scripts that inspect the target repo and exit outside scope. =notify-emacs-session-end.sh= and =notify-emacs-notification.sh= escape interpolated buffer-name values before embedding them in Elisp strings; =load-elisp-after-edit.sh= base64-encodes the changed file path and decodes it via =base64-decode-string= on the Elisp side so command-substitution metacharacters never reach the shell during interpolation. =lib-repo-root.sh= extracts a leading =cd <path>= from the bash command via a bash-regex match (bailing out on any =$(...)= or backtick) and calls =git -C <path>= rather than re-shelling the command; when the hook payload contains a structured =cwd= or =workdir=, that directory is used as the fallback repo context before the hook process cwd.

Claude and Codex hooks are no longer shared through adapters.  Claude hooks live
in =claude/hooks/=; Codex counterparts live in =codex/hooks/=.  The mapping and
known non-pairs are recorded in =ai-config-sync.json=.

*** require-elisp-test-before-commit.sh (PreToolUse)
:PROPERTIES:
:ID:       4E0CDC5D-C436-4701-AC99-E29F950318B4
:END:

Blocks =git commit= when Elisp files (=*.el= or =emacs/config.org=) are staged in the command's target repo but no test has been run in the current session. Forces the agent to verify changes before committing. The error message suggests a ready-to-paste =batch-test.sh= command with the detected package name. Also detects the case where =git add= and =git commit= appear in the same bash command (which would otherwise bypass the staged-file check by running before staging occurs). When scanning the command string, only the =git add= arguments are examined (not the commit message or other parts of the command) to avoid false positives from commit messages that mention =.el= files.

*** track-elisp-test.sh (PostToolUse)
:PROPERTIES:
:ID:       85C6B926-9F3F-431A-9650-F504CEE12B4A
:END:

Creates a marker file (=/tmp/claude-elisp-tested-{SESSION_ID}=) whenever =emacs --batch= runs. This marker is what the pre-commit hook checks for. The marker is consumed (deleted) after a successful commit, so each commit requires a fresh test. Also verifies that the test loaded files from the working tree, not stale =.elc= from =elpaca/builds/=: if the command output contains evidence of a stale load (e.g. "using older file" or =elpaca/builds/*.elc=), the marker is NOT created and a warning is printed, so the commit hook will still block. Supports both Claude Code's =tool_output= payload and Codex's =tool_response= payload.

*** require-readme-update.sh (PreToolUse, dotfiles-scoped)
:PROPERTIES:
:ID:       F4D78958-4194-4DB3-9624-C04EC7790960
:END:

Blocks =git commit= when files in =claude/= (skills, hooks, settings, CLAUDE.md) are staged in the command's target repo but =claude/README.org= is not. Also detects when =git add= and =git commit= appear in the same bash command (which would otherwise bypass the staged-file check). Ensures documentation stays in sync with configuration changes. Project-specific skills (under =.claude/skills/=) are excluded from this check, since they belong to their project's documentation, not the dotfiles README. Runtime data paths (=digests/=, =last-run/=, =twitter-vet/vetted/=) are also excluded, since these are skill output files that don't warrant a README update.

*** remind-claude-readme-update.sh (PostToolUse)
:PROPERTIES:
:ID:       2D9A0367-26C4-40B8-9B9C-06346D89238B
:END:

Companion to =require-readme-update.sh=. Fires globally after any Write or Edit, resolves the target path through symlinks, and outputs a reminder if the file is under =dotfiles/claude/= but the agent is working in a different project. This closes the gap where the commit-blocking hook never fires because the agent never switches to the dotfiles repo to commit. Silent when editing =README.org= itself or when already in the dotfiles repo. The Claude hook emits the reminder as plain stdout; the Codex counterpart wraps the same reminder as PostToolUse JSON because Codex treats plain stdout as invalid hook protocol output.

*** remind-ai-config-sync.sh (PostToolUse)
:PROPERTIES:
:ID:       44F44D89-3847-49A8-BD92-380CD88CC0B1
:END:

Fires after Claude Edit or Write operations and calls =bin/ai-config-sync
remind-claude=. If a global or project-local Claude instruction file, skill,
hook, or live settings file changed, it prints the Codex counterpart that should
be updated in the same session. Project-local paths use the target repo's
sibling =CLAUDE.md=/=AGENTS.md= pairs and =.claude/= and =.codex/= trees. This is
intentionally a reminder, not an auto-sync mechanism.

*** require-ai-config-sync.sh (PreToolUse)
:PROPERTIES:
:ID:       5428CDD1-C447-4549-8365-56066C479F61
:END:

Fires before Bash =git commit= commands and calls =bin/ai-config-sync
guard-commit=. Blocks one-sided Claude/Codex configuration changes in the
dotfiles repo unless the counterpart is synchronized or =ai-config-sync.json=
records an artifact-specific status or exemption. It also resolves the git repo
targeted by the commit command and applies the same checks to project-local
sibling =CLAUDE.md=/=AGENTS.md= instruction files, =.claude/= and =.codex/= skills,
hooks, and hook registration files. For skills, the guard checks body and
auxiliary-file parity after removing tool-specific frontmatter from both sides,
so unrelated counterpart edits do not satisfy it. This keeps the model explicit:
the active agent makes both edits and commits them together, or records an
intentional global exception in the manifest.

*** require-doc-update.sh (PreToolUse)
:PROPERTIES:
:CUSTOM_ID: h:require-doc-update
:ID:       BB6FE0BD-FC74-4BEB-89AB-4FE902A84055
:END:

Blocks =git commit= when =.el= files are staged but no =doc/*.org= or =README.org= manual update is included. Applies only in repos that have a =doc/= directory or a =README.org= at the root. When scanning a combined =git add && git commit= command, only the =git add= arguments are examined (not the commit message) to avoid false positives from commit messages that mention =.el= files. Verifies that doc files have actual modifications (staged or unstaged) rather than accepting unchanged files. If a staged org manual, or an org manual in the same command's =git add= arguments, has Texinfo export headers and dirty generated =.texi= or tracked =.info= outputs, blocks the commit until those generated files are staged too. Ensures documentation stays in sync with code changes.

*** block-elpaca-rebuild-uncommitted.sh (PreToolUse)
:PROPERTIES:
:CUSTOM_ID: h:block-elpaca-rebuild-uncommitted
:ID:       C3D0F41C-1A34-4AFE-BB9F-FC803D557CCF
:END:

Blocks =emacsclient= commands that invoke =elpaca-rebuild= or =elpaca-extras-reload= when there are uncommitted =.el= changes in =emacs/extras/=. The elpaca source clone only has committed code, so rebuilding before committing would silently compile and load stale definitions from the last commit, ignoring the working tree edits. The error message lists the uncommitted files and instructs the agent to commit first.

*** block-elpaca-dotfiles-edit.sh (PreToolUse)
:PROPERTIES:
:ID:       864BDC53-69C3-45F3-BB74-68154834BD4B
:END:

Blocks Edit and Write operations targeting files under =elpaca/sources/dotfiles/=. The elpaca clone is a separate git checkout that syncs from the canonical =~/My Drive/dotfiles/= repo via git commits; direct edits there would be overwritten on the next sync. Directs the agent to edit the canonical files instead.

*** load-elisp-after-edit.sh (PostToolUse)
:PROPERTIES:
:CUSTOM_ID: h:load-elisp-after-edit
:ID:       F07EF468-2770-42DE-B282-9BCB6E624EF5
:END:

Fires on every Edit or Write of an =.el= file under =elpaca/sources/= or =dotfiles/emacs/extras/=. Identifies the elpaca package (by matching the file's basename against package names for extras, or by source directory prefix for standalone packages), then runs =elpaca-rebuild= + =elpaca-wait= + =elpaca-extras-reload= synchronously via =emacsclient=. This byte-compiles the source into =elpaca/builds/=, waits for completion, and reloads all features. Skips byte-compiled files and test files. Note: for dotfiles extras edits, the rebuild may compile from a stale elpaca source if the edit hasn't been committed yet; the =post-commit= hook triggers a second rebuild from the correct source (see =emacs/extras/doc/elisp-development-workflow.org=).

*** regenerate-manual-after-edit.sh (PostToolUse)
:PROPERTIES:
:ID:       59760DD8-5F48-4F71-BDCF-68B74D4DBB5F
:END:

Regenerates Texinfo manuals after Edit or Write touches an org source with =#+texinfo_filename= or =#+export_file_name= headers. This covers agent edits that bypass package-local =after-save-hook= entries. The hook runs =ox-texinfo= in a clean batch Emacs with lockfile creation disabled, so stale or active locks on generated =.texi= outputs do not make batch export fail. It uses the exact =.texi= path returned by =org-texinfo-export-to-texinfo= and runs =makeinfo --no-split= only for the matching =.info= file when that file already exists in the repository layout.

*** elpaca-run.sh (utility)
:PROPERTIES:
:ID:       54F62C33-E882-4B75-A4F0-A8963E5AF345
:END:

Resolves the active elpaca profile dynamically (via =emacsclient -e 'init-current-profile'=) and runs a script from the specified elpaca package. Searches both =repos/= and =sources/= directories to handle different elpaca layouts. Used by the Notification and Stop hooks to invoke =claude-code-hook-wrapper= without hardcoding the profile name. The =emacsclient= call has a 5-second timeout to prevent hangs when the Emacs server is unresponsive.

*** notify-emacs-session-end.sh (Stop)
:PROPERTIES:
:ID:       8A2F3E91-4D5C-4B8F-9E3A-7C6D2A1F0B94
:END:

Forwards the session-end event to Emacs via =claude-code-handle-hook=. Runs through =fire-and-forget.sh= so the CLI doesn't block on =emacsclient=. Works together with =agent-log-sync-on-session-end= to trigger log syncing when a session ends.

*** claude-code-hook-wrapper (Notification, Stop)
:PROPERTIES:
:ID:       44E20BF0-9B6B-420E-858F-98CBF18D13F8
:END:

Invoked via =elpaca-run.sh= on Notification and Stop events. Delegates to the =claude-code= Emacs package's hook wrapper, which updates the Emacs-side state (e.g. mode-line indicators) in response to Claude Code lifecycle events.

*** block-texi-edit.sh (PreToolUse)
:PROPERTIES:
:ID:       40DE3EB3-0452-4AD7-8CA4-DE80BEF0F6B0
:END:

Blocks edits to =.texi= files, which are auto-generated from =.org= sources via =ox-texinfo=. Directs the agent to edit the corresponding =.org= file instead and regenerate.

*** block-sensitive-read.sh (PreToolUse)
:PROPERTIES:
:ID:       8B4D91E7-7EA8-44FC-83E9-FD92097997BA
:END:

Guards against accidental ingestion of sensitive files into the context window. Fires on =Read= AND =Bash=, checking either the target =file_path= or the Bash command for the same sensitive-path set: =~/.password-store/=, =.zshenv-secrets=, =~/.ssh/id_*= (private keys), =~/.gnupg/=, =tokens.json= inside =~/.config/=, =~/.gmail-mcp-epoch/credentials/=, and =secret.json= / =client_secret*.json= inside =~/.config/=. The Bash branch allowlists operations that legitimately mention these paths without exposing content: auth-aware tools (=pass=, =op=, =git-crypt=, =security=), metadata commands (=ls=, =stat=, =file=, =basename=, =dirname=, =realpath=), =wc= with safe flags, =grep=/=rg=/=ripgrep= with non-content flags (=-l=, =-c=, =-q=, =-L=), and existence tests (=[ -f=, =[[ -f=, =test -f=). Anything else that touches a sensitive path is treated as content-extraction and escalated. Uses =permissionDecision: "ask"= to escalate to the user rather than hard-denying. Closes the structural gap that allowed e.g. =grep ANNAS .zshenv-secrets= to dump the secret value into the transcript via Bash, bypassing the original Read-only matcher.

*** block-secret-leak.sh (PreToolUse)
:PROPERTIES:
:ID:       1F459B3F-EA5B-4FF3-BE3B-B963D8C0F0DB
:END:

Compensating control for permissive modes (=auto=, =bypassPermissions=). Fires on =Bash|Write= and pattern-matches the tool input for common secret formats: AWS access keys, GitHub tokens, GitLab tokens, Slack tokens, generic =sk-= API keys, Anthropic keys (=sk-ant-...=), Stripe live/test keys (=sk_live_=/=pk_live_=/=sk_test_=/...), Linear keys (=lin_api_...=), Airtable personal access tokens (=pat<14>.<64-hex>=), Google API keys (=AIza...=), JWTs, database connection strings with embedded credentials, private key headers, Google OAuth client secrets, and generic high-entropy values assigned to known secret variable names. Blocks the operation with a specific message identifying the secret type. Allowlists legitimate secret-handling commands (=pass=, =op=, =security=, =git-crypt=), pattern-scanning tools (=grep=, =rg=), environment variable references (=$VAR=), and writes to known secret files (=.zshenv-secrets=, =.env.op=). Also detects exfiltration patterns: sensitive files (SSH keys, GPG keys, password store, =tokens.json=, =.zshenv-secrets=) piped to network tools (=curl=, =wget=, =nc=, =ncat=), =curl --data @= uploads of sensitive files, and network commands containing inline high-entropy strings (>30 chars with mixed character classes including digits). Complements the =/security-audit= skill, which audits the broader security posture on demand rather than blocking in real time.

*** block-destructive-command.sh (PreToolUse)
:PROPERTIES:
:ID:       DC12DCB5-4C06-43D2-B235-9C08B99653D9
:END:

Blocks or gates destructive shell commands that are hard to reverse. Hard-blocks (deny): =rm -rf= (suggests =trash=), =git push --force=, =git reset --hard=, =git clean -f=, =git checkout -- .=, and =git branch -D=. Prompts for confirmation (ask): =git push= (all pushes), =git clone= / =gh repo clone=. Each pattern includes a specific remediation suggestion. Users can run blocked commands manually via =!=.

*** guard-external-actions.sh (PreToolUse)
:PROPERTIES:
:ID:       963E9C95-5AA8-4905-905E-F5C0FF68627E
:END:

Deny-by-default guard for all MCP tools. Fires on all =mcp__= tools. Read-only tools are explicitly allowlisted (pattern-matched on verb prefixes like =read_*=, =list_*=, =get_*=, =search_*=, plus specific tool names per server). Everything else exits 2 to require user confirmation. This ensures new MCP servers and tools are guarded automatically without needing to enumerate every dangerous action.

*** require-elisp-skill.sh (PreToolUse)
:PROPERTIES:
:ID:       B9B181F6-1530-437D-8B3C-848E4C65B28F
:END:

Blocks edits to =.el= files until the =elisp-conventions= skill has been loaded in the current session. Checks for a session marker created by the companion =track-elisp-skill.sh= hook. If no marker exists, denies the edit with a message telling the agent to load the skill first.

*** track-elisp-skill.sh (PostToolUse)
:PROPERTIES:
:ID:       1C627F38-97A8-447E-95A0-586890A4D5B8
:END:

Creates a session marker (=/tmp/claude-elisp-skill-{SESSION_ID}=) when the =elisp-conventions= skill is loaded via the Skill tool. This marker allows =require-elisp-skill.sh= to permit =.el= edits. Cleans up stale markers older than 2 hours.

*** require-elisp-verify-after-commit.sh (PreToolUse)
:PROPERTIES:
:ID:       130ACA78-30B6-4B1A-9E02-EC99B126079D
:END:

Blocks all Bash commands (except =emacsclient= and =batch-test.sh=) after committing =.el= files until verification via =emacsclient -e= has been run. Uses a session marker created by the companion =track-elisp-verify.sh= hook. Ensures the agent verifies that committed Elisp loads correctly in the running Emacs before moving on.

*** track-elisp-verify.sh (PostToolUse)
:PROPERTIES:
:ID:       CEA9EC73-B53B-4097-B9CA-38828EB94D97
:END:

Creates a verification marker (=/tmp/claude-elisp-verify-needed-{SESSION_ID}=) after a successful =git commit= whose target repo's just-created commit includes =.el= files. Clears the marker when =emacsclient -e= runs. Skips test files (=*-test.el=, =*-tests.el=). Cleans up stale markers older than 2 hours.

*** notify-emacs-notification.sh (Notification)
:PROPERTIES:
:ID:       E0B2AF05-8A03-40DE-B373-2952381AA760
:END:

Forwards Claude Code notifications to Emacs via =claude-code-handle-hook=. Runs through =fire-and-forget.sh= so the CLI doesn't block on =emacsclient=.

*** fire-and-forget.sh (utility)
:PROPERTIES:
:ID:       4DAD911D-7415-47DC-B7ED-62F0A2C22017
:END:

Wrapper that runs a hook command in the background. Reads stdin first to avoid broken pipes, then execs the command detached. Used by the Notification and Stop hooks so the CLI doesn't block waiting for =emacsclient=.

*** lib-repo-root.sh (utility)
:PROPERTIES:
:ID:       6149F4DB-9E5E-496D-9976-08EE7A930DAF
:END:

Shared helper sourced by other hooks. Resolves the git repo root from the command context. PreToolUse hooks run before the bash command executes, so the hook's cwd may differ from the command's effective cwd. If the command starts with =cd <path> &&=, this helper resolves the repo root from that path and cds into it so subsequent git commands operate on the correct repo. Otherwise it uses structured =workdir= / =cwd= fields from the hook payload when present, then falls back to the hook's current working directory.

*** lib-staged-files.sh (utility)
:PROPERTIES:
:ID:       8BC3FC4A-63DE-4E4A-9174-C6D8672C5EC0
:END:

Shared helper sourced by other hooks. Lists staged files and handles the =--amend= flag correctly by comparing against =HEAD~1= when amending (since amending replays the previous commit's changes).
