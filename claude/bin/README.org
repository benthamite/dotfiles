#+title: Claude helper commands
#+author: Pablo Stafforini

* Activation and scope

This directory contains Claude-specific service wrappers, test runners, and
configuration installers. There is no =~/.claude/bin= link, and the directory is
not an executable root by itself. Agent instructions and other scripts invoke
these files by their repository paths. A similarly named command elsewhere on
=PATH= is not evidence that the tracked file is active.

* Operational boundaries

=ai-provider-key= keeps provider administrator credentials out of project
repositories and stores project-scoped runtime credentials in the Automations
vault. Non-dry operations require a current, authorized spend-control assertion
within the retained helper ceiling; the helper neither reads live limits nor
enforces per-key budgets. Pure dry-run mode prepares metadata without account
access. Provisioning preflights local/item conflicts, uses broker stdin for
secret writes, and compares exact readback before wiring an immutable item
reference. Provider, vault and local-file changes are not one transaction;
reconcile an uncertain attempt before retrying or replacing a credential.

=slack.py= uses the selected operator's browser session, not an automation bot.
Its Epoch credential path uses =op-automations=; credential-bearing API/file
requests refuse redirects and omit response payloads from failure diagnostics.
Success payloads can still contain private data or newly created app secrets:
callers must capture and selectively consume them, not print them wholesale.

=ahrefs-api-guard= probes the free subscription-usage endpoint before every
paid request. It requires a positive conservative =--estimated-cost= and valid
workspace/API-key quota, then compares the projected balance with the configured
reserve and key allowance. This preflight is not an atomic reservation or hard
spending ceiling. Manual Ahrefs calls must use the guarded command; a similarly
named PATH wrapper should not be assumed to be this tracked file without
resolving it.

=copy-slack-draft= takes the draft from =--stdin= or a private UTF-8 =--file=
outside Drive, and finds the running Emacs server's socket unless =--socket=
names another. It opens the exact Epoch destination, preserves
conflicting unsent input, verifies composer insertion, then stages the same text
in the kill ring without importing or exporting the system clipboard.
=--open-only= opens without copying or inserting. Structured receipts distinguish
acknowledged stages from uncertain outcomes; private recovery snapshots survive
uncertainty. Raw request errors and draft contents are omitted from diagnostics.

=cloudflare-deploy-token= reads an owner-only JSON file (mode 600) at
=~/.config/agent-tools/cloudflare-deploy-token.json=. Keep its directory mode
700 and outside shared repositories or sync roots. Set
=CLOUDFLARE_DEPLOY_TOKEN_CONFIG= to use another private file. The file requires
exactly three nonempty string fields: =default_account_id=,
=automation_admin_reference=, and =desktop_admin_reference=. The references
use =op://VAULT/ITEM/FIELD= syntax; store references, never credential values.
=--account-id= overrides the default account. Missing, malformed or broadly
readable configuration stops before any broker or provider call. The helper
tries the automation reference first, then explicitly reports a fallback to the
desktop reference if that read fails. Scope remains Workers Scripts Write,
with =--kv= and =--d1= adding their respective permissions. Both create and
update send the minted value through broker stdin, never command arguments.
Update preserves the existing API credential item's other fields and rejects
unsupported categories, attachments, passkeys or ambiguous credential fields
before minting. Configuration symlinks and nonregular files are refused.

* File index

| File | Purpose |
|------+---------|
| =_gworkspace_auth.py= | Internal OAuth and short-lived token-cache helper shared by the Gmail and Sheets wrappers. |
| =ahrefs-api-guard= | Performs the required free quota preflight before a paid Ahrefs API request. |
| =ai-provider-key= | Provisions and stores project-scoped provider runtime keys while keeping administrator credentials separate. |
| =apply-auto-mode= | Installs the tracked =claude/auto-mode.json= classifier block into the live =~/.claude/settings.json= with a timestamped backup; =--check= reports drift. Run by the user, not from an agent session. |
| =ai-provider-key-from-clipboard= | Consumes a console-created Anthropic key without exposing it; OpenAI import is refused, and help/dry-run never reads the clipboard. |
| =store-secret-from-clipboard= | Consumes any console-created API credential from the clipboard, validates its shape, and creates or updates a 1Password item through =op-desktop= on stdin without putting the value in arguments. Updates preserve other fields and reject attachments, passkeys or ambiguous field matches. |
| =batch-test.sh= | Loads canonical package source in batch Emacs, rejects stale-load warnings, and emits repository/package/source-revision evidence with a one-time replay-correlation token. |
| =check-elpaca-sync-hook= | Validates the embedded Elpaca sync hook and confirms that the installed hook is identical. |
| =cloudflare-deploy-token= | Mints a per-project Cloudflare Worker-deploy API token using privately configured administrator references and stores it in the Automations vault. |
| =copy-slack-draft= | Opens an exact Epoch Emacs target or stages a draft (stdin or private file) in its composer and kill ring, resolving the running server's socket by default, with per-stage receipts. |
| =cr-worktree-gc.sh= | Removes only clean, fully pushed transient CR review worktrees and preserves unfinished work. |
| =elisp-ert= | Runs profile-aware ERT for a package, test file, and optional test selector. |
| =elisp-check-evidence= | Runs a tracked project-owned non-package Elisp check against the working tree or an explicit index snapshot and emits repository/file/source-revision evidence with a one-time replay-correlation token. |
| =elisp-live-verify= | Waits for a committed package reload or safely removes a deleted package build, requires a non-nil check naming the resolved Elpaca ID, and emits canonical repository-label/commit evidence with a one-time replay-correlation token. |
| =elisp-source-revision= | Computes the Git-and-content identity used to bind Elisp test evidence to the current source. |
| =elpaca-rebuild-wait= | Resolves a package ID or checkout label, rebuilds and reloads the canonical Elpaca ID with bounded polling, or observes exact source/mirror-bound dotfiles post-commit reload state. An observer can resume certification of an abandoned request under its free package lock, retaining and verifying the original token, runtime, source and status identity without requesting another rebuild. |
| =gmail.py= | Provides the multi-account Gmail read, search, draft, send, reply, attachment, and archive operations. |
| =kill-ring-put= | Stages non-secret text from stdin or a file in the running Emacs kill ring in one command, optionally also on the macOS clipboard, without echoing the payload. |
| =install-skills.sh= | Installs the live interactive and programmatic skill links, then applies skill pruning and private shims. |
| =op-clipboard-store= | Stores a validated console-copied secret through broker stdin with optional account pinning; checks ID-bound concealed byte-equal readback and refuses existing Login JSON edits that cannot preserve passkeys. Desktop authorization may prompt. |
| =sheets.py= | Provides the multi-account Google Sheets read, write, append, clear, sheet-management, and metadata operations. |
| =slack.py= | Provides multi-workspace Slack search, history, thread, user, saved-item, and read-state operations. |
| =sync-mcp-servers.sh= | Copies canonical user MCP definitions into account configurations while preserving account-specific environments. |
| =triage-notes= | Locates the shared Org task engine and binds supported commands and loaded artifacts to the personal corpus, rejecting known Epoch roots; completion is a prepared update. |
| =update-gworkspace-refresh-token= | Refreshes and stores a selected Google Workspace account token without printing the token value. |

The documentation audit requires every tracked direct child to appear exactly
once in this table. Several wrappers obtain credentials from external stores or
perform remote writes. Inspect the requested account and operation before use,
and never invoke a credential path merely to verify this index.
