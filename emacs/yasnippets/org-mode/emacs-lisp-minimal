# -*- mode: snippet -*-
# name: emacs-lisp-minimal
# key: elm
# contributor: Pablo Stafforini
# --
#+begin_src emacs-lisp :results silent :tangle (when (memq version '(minimal)) (print paths-file-init))