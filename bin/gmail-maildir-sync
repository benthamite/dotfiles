#!/bin/bash
# Entry-point wrapper for gmail-maildir-sync Python package.
export PYTHONPATH="$HOME/My Drive/repos/gmail-maildir-sync${PYTHONPATH:+:$PYTHONPATH}"
exec python3 -m gmail_maildir_sync "$@"
