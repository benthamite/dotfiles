\input texinfo    @c -*- texinfo -*-
@c %**start of header
@setfilename elpaca-extras.info
@settitle elpaca-extras: Extensions for Elpaca
@documentencoding UTF-8
@documentlanguage en
@c %**end of header

@dircategory Emacs misc features
@direntry
* Elpaca Extras: (elpaca-extras). Extensions for Elpaca.
@end direntry

@finalout
@titlepage
@title elpaca-extras: Extensions for Elpaca
@author Pablo Stafforini (@email{pablo@@stafforini.com})
@end titlepage

@contents

@ifnottex
@node Top
@top elpaca-extras: Extensions for Elpaca

This manual describes the features and customization options for the Emacs Lisp file @samp{elpaca-extras.el}.

@end ifnottex

@menu
* Overview::
* User options::
* Commands::
* Indices::

@detailmenu
--- The Detailed Node Listing ---

User options

* @code{elpaca-extras-write-lock-file-excluded}::

Commands

* Reload::
* Update and reload::
* Rebuild and reload::
* Lock file management::

Indices

* Function index::
* Variable index::

@end detailmenu
@end menu

@node Overview
@chapter Overview

@samp{elpaca-extras.el} extends the @samp{elpaca} package manager with commands that make it easier to iterate on packages without restarting Emacs. When you update or rebuild a package, its old definitions remain loaded in the running session. These extensions automate the tedious process of force-loading updated features, and they also provide filtered lock-file writing.

The main capabilities are:

@itemize
@item
@strong{Reload after update.} A command that updates a package and automatically reloads its features once the build finishes (@ref{Update and reload}).
@item
@strong{Reload after rebuild.} The same workflow for package rebuilds (@ref{Rebuild and reload}).
@item
@strong{Manual reload.} A standalone reload command for when you have made local edits to a package's source (@ref{Reload}).
@item
@strong{Filtered lock files.} Writing Elpaca lock files while excluding specific packages (@ref{Lock file management}).
@end itemize

@node User options
@chapter User options

@menu
* @code{elpaca-extras-write-lock-file-excluded}::
@end menu

@node @code{elpaca-extras-write-lock-file-excluded}
@section @code{elpaca-extras-write-lock-file-excluded}

@vindex elpaca-extras-write-lock-file-excluded
The user option @code{elpaca-extras-write-lock-file-excluded} specifies a list of package identifiers (symbols) that should be omitted when writing a lock file with @code{elpaca-extras-write-lock-file-excluding} (@ref{Lock file management}). This is useful for packages that are installed from local sources or that you do not want pinned to a specific revision.

The default value is @code{nil} (no exclusions). The type is a list of symbols.

@node Commands
@chapter Commands

@menu
* Reload::
* Update and reload::
* Rebuild and reload::
* Lock file management::
@end menu

@node Reload
@section Reload

@findex elpaca-extras-reload
After making local edits to an installed package or after a manual rebuild, the old feature definitions remain active in the running Emacs session. The command @code{elpaca-extras-reload} force-loads the features provided by a specified package so new definitions replace old ones.

It works by reading the package's @samp{.el} files, extracting every feature name and direct dependency from their @code{provide} and @code{require} forms, and loading each selected source or compiled artifact once. The main feature retains its established first-load position; package subfeatures are then loaded after the subfeatures they require. This dependency order keeps unchanged EIEIO class hierarchies coherent during reload. Before loading, the command compares statically visible classes' direct parent sequences and effective instance-slot vectors with the selected definitions. It refuses a selected file when that file previously created a class whose declaration cannot be inspected. A second guard watches the actual EIEIO class definitions during @code{load}, so macro-generated, conditional, and compiled definitions cannot bypass the layout check. Class-allocated slots and overrides that preserve the instance layout remain reloadable. If inheritance or the instance layout changed, the command stops before mutating that class and reports that Emacs must be restarted; redefining it would reinterpret live instance vectors and corrupt their state. If only the runtime guard could discover the unsafe definition, earlier non-class forms in the artifact may already have run, which is another reason to restart after that failure. Asynchronous build-and-reload records this outcome, and any other reload error, as a terminal failure instead of allowing an error to escape from the completion hook. By default, features that were already loaded are loaded again, and the main package feature is also loaded so locally rebuilt packages do not remain in an autoload-only state. With a prefix argument, all features provided by the package are loaded regardless of their previous state.

@node Update and reload
@section Update and reload

@findex elpaca-extras-update-and-reload
The command @code{elpaca-extras-update-and-reload} combines updating a package via Elpaca with an automatic reload. It prompts for a queued package, triggers @code{elpaca-update}, and installs a temporary hook on @code{elpaca-post-queue-hook} to detect when the build finishes. If the build succeeds, it calls @code{elpaca-extras-reload} (@ref{Reload}) and reports the result. If the build fails, it directs you to the @samp{*elpaca-log*} buffer for details.

This provides a convenient single-step workflow: update, build, and force-load the new definitions.

The command returns a status token after enqueueing the build. Reloading is driven by @code{elpaca-post-queue-hook} when Elpaca's build process actually finishes. Callers that need a deterministic completion point should poll @code{elpaca-extras-build-reload-status} or @code{elpaca-extras-format-build-reload-status} for that token with short calls; they should not call @code{elpaca-wait} inside the daemon.

@node Rebuild and reload
@section Rebuild and reload

@findex elpaca-extras-rebuild-and-reload
The command @code{elpaca-extras-rebuild-and-reload} is analogous to @code{elpaca-extras-update-and-reload} (@ref{Update and reload}) but uses @code{elpaca-rebuild} instead of @code{elpaca-update}. This is useful when you have made local source changes and want to rebuild the package from those changes rather than fetching upstream updates.

Build enqueueing and the later asynchronous autoload and compile steps use unbounded printing, so subprocess forms containing long dependency-path lists are serialized completely even when the live Emacs has customized @code{print-length} or @code{print-level}.

@node Lock file management
@section Lock file management

@findex elpaca-extras-write-lock-file-excluding
The command @code{elpaca-extras-write-lock-file-excluding} writes an Elpaca lock file to the specified PATH while filtering out packages listed in @code{elpaca-extras-write-lock-file-excluded} (@ref{@code{elpaca-extras-write-lock-file-excluded}, , User options}). When called interactively, it prompts for the output file path.

The optional ELPACAS argument accepts a queue-like list as used by @code{elpaca-write-lock-file}. When nil, the current queue is used. This makes it possible to call the function programmatically with a custom package set.

@node Indices
@chapter Indices

@menu
* Function index::
* Variable index::
@end menu

@node Function index
@section Function index

@printindex fn

@node Variable index
@section Variable index

@printindex vr

@bye
